# 版本紀錄 (Changelog)

本專案採用 [語意化版本](https://semver.org/lang/zh-TW/) (Semantic Versioning)：
`主版本.次版本.修訂版本`，例如 `0.1.0`。

## [0.28.0] - 2026-07-30

首頁整個重新設計，回應使用者「不要零零散散、不要很花、要有點專業、不要像硬塞進去空間很小
功能不齊」的具體要求。跟使用者一起討論了幾個方向 (Sunsama/Todoist 式時間軸、Notion 式模組化
極簡) 後，選定「待辦事項為主、模組化極簡」的方向，並根據討論過程中的追加需求逐步調整，最後
定案。這是一次架構層級的改動：桌面版跟手機版過去是完全獨立的兩套版面 (`.dash-grid` 三欄卡片 +
`.dash-mobile-widgets` 拼貼)，這次統一成同一份單欄排版，用 CSS 依螢幕寬度微調間距字級，不再
分兩套邏輯各自維護。

- **待辦事項改成主角，行事曆自動併入同一條清單**：不再是「待辦事項卡片」跟「行事曆卡片」各自
  獨立、內容還會重複的兩塊，而是把待辦事項跟接下來兩週的行事曆行程合併成一條清單，用細線分隔
  (而不是卡片框線)，行事曆來源的項目用一個很淡的「行事曆」標籤區分，其餘呈現方式完全一致。
- **排序邏輯改成「離現在最近」**：不是單純依日期先後排，而是依照與目前這個時間點的距離排序——
  不論是快到期還是即將發生，越接近現在這一刻的事情，就越靠近清單最上面，逾期的項目維持紅色
  「逾期」標籤，即將發生 (3 小時內) 的項目有綠色「即將」標籤，一眼就能分辨輕重緩急。也保留了
  依優先度排序的切換按鈕。
- **統計條**：完成度、已逾期件數、來自行事曆的件數，三個數字取代原本「進度環/分組統計/今日焦點」
  三選一的樣板系統——那套樣板是上一版做的，這次發現它其實跟「不要零零散散」的目標相衝，所以拿掉，
  改成單一、固定、資訊量剛好的呈現方式。
- **心情可以直接在首頁打卡**：沿用既有的 1-10 分快速評分功能，搬到首頁待辦清單正下方，不用先點
  進心情分頁。
- **問候語加上每次進來都不一樣的一句話**：沿用系統原本依時段 (晨間/午後/傍晚/深夜) 準備的鼓勵
  語句庫，改成用引言的樣式獨立呈現在「哈囉，OO」下面，加上一個暖色系的邊框強調，跟上面純資訊性的
  「今天有 N 件事在等你」副標題區分開來。「哈囉」的字級跟顏色也改成比較溫馨的暖棕色調。
- **股票/記帳/帳單/日記/交通旅遊/本週動態收進「更多」清單**：改成單欄的細線分隔清單列，取代原本
  一張一張獨立的卡片，視覺上不再「疊在一起看起來很雜亂」。「自訂」功能維持原本可以開關/拖曳排序
  的機制不變。
- **手機版新增「全部功能」捷徑格**：原本只有手機拼貼版才有的完整功能捷徑格，維持在新版面最下方，
  桌面版也看得到 (原本桌面版沒有這塊，只能靠左側選單，這次補齊)。
- 已用 Playwright 確認：14 個分頁 (含新首頁) 在桌面 (1280px) 跟手機 (390px) 寬度下都沒有任何
  橫向捲動，跟過去的規範一致。

## [0.27.1] - 2026-07-30

回應使用者反饋「首頁很亂、代辦事項跟行事曆重複、打勾符號很躁、想要現代感」，這次做的都是
根據具體回饋的修正 (完整的首頁排版重做是下一步，正在跟使用者討論方向)：

- **修掉一個真正的重複顯示問題**：行事曆 API 本來就會自動把待辦截止日整合進去顯示 (方便在
  行事曆分頁一次看到全部行程)，但首頁的「行事曆行程」卡片跟「待辦事項」卡片剛好都在顯示
  同一批「待辦截止日」資料，兩張卡片撞在一起看起來像同一件事講兩次。這次把首頁 (含手機版
  拼貼) 的行事曆卡片改成只顯示真正的行事曆行程/帳單/生日，不重複顯示待辦事項卡片已經有的內容。
- **待辦事項卡片改用細圓框勾選**：原本是瀏覽器原生的方形 checkbox，樣式跟卡片其他自訂元件
  放在一起顯得突兀老氣；改成自己畫的細圓框，勾選時填滿主色＋打勾符號，質感一致許多。
- **首頁待辦卡片可以直接刪除、切換排序**：每筆待辦旁邊多一個淡淡的「×」可以直接刪除
  (不用先跳去待辦事項頁)；清單上方加了「依時間／依優先度」切換，自己決定看的順序；
  每筆也把到期日期/是否過期用小字清楚標示出來 (今天/已過期/幾號)，不用點進去才知道。
- **版本號手機版也看得到**：原本版本號只顯示在側欄最下面，手機版側欄整個是隱藏的，
  完全看不到目前是第幾版。這次加進手機版的「更多」選單最下面，跟系統狀態連結放一起。

## [0.27.0] - 2026-07-30

回應使用者問「待辦事項可以怎麼優化，包括 UI」——待辦事項原本只能新增/勾選完成/刪除，
沒有編輯功能，優先度只靠一個小 emoji 分辨，清單攤平依期限排序看不出「今天真的該做的」
是哪幾件，首頁的待辦卡片也只有兩行文字。這次做了全面的重新設計，並且針對「首頁待辦卡片」
做了三種視覺模板讓使用者實際比較後選定：

- **編輯既有任務**：待辦事項清單每一筆現在都有「✏️ 編輯」按鈕，標題/日期/優先度/精力/備註/
  重複/準時提醒都能直接改，不用刪掉重建。表單同時負責新增跟編輯，手機版跟桌面版共用同一套。
- **視覺優化**：任務左側加優先度色條 (紅/黃/綠，跟首頁提醒卡片同一套視覺語言)；已過期的任務
  額外加淡紅底 + 紅字「⚠️ 已過期」標籤凸顯出來，不用細看文字才知道逾期了。
- **依到期日分組顯示**：待完成清單現在分成「已過期／今天／本週內／之後／沒有設定到期日」
  五組，不用在一長串清單裡自己找「今天到底要做哪些」。
- **表單收合＋分頁籤切換**：新增任務表單預設只留標題/日期/優先度，「精力／備註／重複／
  準時提醒」收進「更多選項」收合區塊，手機版不用一次滑過一大串欄位；待完成/已完成清單
  改用分頁籤切換 (跟行事曆、心情頁同一套)，不再兩欄長條堆疊，手機版尤其有感。
- **搜尋/篩選＋批次操作**：新增關鍵字搜尋 (比對標題/備註) 跟依優先度篩選；清單右上角
  「☑️ 批次」可以勾選多筆，一次批次完成或刪除，不用一筆一筆點。
- **過期任務的處理方式**：過期不會被自動清掉 (過期通常只是日期沒抓準，不代表這件事不重要，
  悄悄刪掉會讓人以為資料不見了)——改成兩個自己按的快速處理：每筆過期任務旁邊有「⏩ 延到
  今天」一鍵改期，或者在「⚠️ 已過期」分組標題旁按「☑️ 全選批次處理」，一次把整組過期任務
  帶進批次模式，自己決定要完成還是刪除。
- **首頁待辦卡片：3 種模板讓你實際比較後選定**：做了三種呈現方式的實際 mockup 截圖給你看過
  (A 分組統計條、B 完成進度環、C 今日焦點)，這次照你選的**樣式 B (進度環)**上線為預設，卡片
  右上角有下拉選單可以隨時換成 A 或 C 比較，選定的樣式會記住。進度環另外做了視覺打磨：
  改用 SVG 畫圓環 (比原本的 CSS 色塊更精緻、邊緣圓角)、加上過期件數的紅色小標籤 (原本的
  進度環樣式沒有顯示過期資訊)、清單項目加優先度色點，卡片上可以直接勾選完成，不用先跳頁。
- **修正一個潛在的日期判斷 bug**：程式碼裡意外重複定義了兩次「今天」這個函式，其中一個是用
  UTC 時間算的 (另一個用瀏覽器本地時區算)，因為 JS 允許重複宣告、後面那個會蓋掉前面的，
  導致全站「今天」實際上一直是用 UTC 判斷——這在特定時段/時區下，「今天的心情記錄」「日記
  預設日期」「行事曆選到今天」「待辦事項到期/過期判斷」都可能誤判成前一天或後一天。已經
  移除重複的定義，確保全站都用瀏覽器本地時區判斷「今天」。
- **另外修了一個首頁的資料不會更新的 bug**：切到別的分頁再切回首頁時，原本首頁完全不會
  重新整理，帳單/待辦/行事曆這些摘要卡片會一直停在剛登入當下的舊資料，即使背後資料已經
  改變也看不出來，要整頁重新整理 (F5) 才會恢復。這是在測試這次的待辦卡片改版時意外發現的，
  一併修掉了。
- 全部用 Playwright 測過：編輯/分組/搜尋/批次/延期一次過；首頁三種模板切換、進度環正確
  顯示過期件數、切樣式後重新整理頁面確認會記住選擇；手機版 (390×844) 重新測過一輪，
  版面跟互動都正常；14 個分頁的橫向溢出回歸測試也重跑過，全部正常。

## [0.26.0] - 2026-07-30

回應使用者反應「跟內建 AI 助理說『2分鐘後提醒我裝水』，時間到了卻沒有跳出推播通知」——
原本待辦事項只有「到期日」(精確到天)，AI 助理也只會抓「哪一天」，沒有能力處理「幾分鐘/幾秒後」
這種需要精準倒數計時的提醒；背景推播的排程也是每 30 分鐘才跑一次、還會比對內容避免重複打擾，
本來就不是設計來處理「準時」這種需求的。這次把「精準時間提醒」補成一個獨立的機制：

- **待辦事項新增「準時提醒」欄位**：跟原本的「到期日」(只到天) 不同，這個是精確到分鐘的時間點，
  時間一到就會主動推播通知，不用等 30 分鐘排程或內容比對。待辦事項頁面新增「🔔 準時提醒」
  下拉選單 (5分鐘/15分鐘/30分鐘/1小時後，或自訂日期時間)，清單上會顯示「將於 X 點提醒」，
  還沒到時間的話可以按「取消提醒」。
- **AI 助理聽得懂「N 分鐘/秒後提醒我」**：跟 AI 助理說「2分鐘後提醒我裝水」「30秒後提醒我」，
  它會判斷這是需要精準倒數的提醒，跳出確認卡片 (一樣要你自己按「加入待辦」才會真的建立，
  AI 不會自己偷偷加)，加入後同時會建立一筆待辦事項 (自動顯示在首頁跟待辦事項頁)，時間一到
  就會推播提醒你。原本「只是記錄哪天要做」的用法 (例如「提醒我禮拜五交報告」) 不受影響，
  還是跟以前一樣只會記到待辦事項，不會額外推播。
- **背景排程新增每 15 秒的精準檢查**：跟原本每 30 分鐘一次的「日常提醒」排程分開，這組是專門
  處理「使用者明確要求某個時間點提醒」的一次性推播，不受內容比對/24小時節流限制，時間到了
  就準時推播，最慢晚 15 秒 (排程檢查間隔)。
- 這個功能仍然依賴「背景推播通知」要先在個人化設定頁開啟才會真的跳出系統通知 (瀏覽器/手機
  層面的限制，沒辦法繞過)；如果還沒開啟，待辦事項還是會照常建立、只是不會跳出通知，介面上
  也會提醒你這件事。
- 用 Playwright 完整測試過：手動設定 20 秒後的提醒，確認背景排程準時把它標記為已推播；
  待辦事項頁面的快速選項/自訂時間/取消提醒都測過；AI 助理的確認卡片 (含正確換算「幾分鐘後」
  的顯示文字) 桌面版跟手機版都截圖確認過；14 個分頁的橫向溢出回歸測試也重跑過，全部正常。

## [0.25.0] - 2026-07-29

延續上一版行事曆手機版排版，這次把行事曆本身的功能補齊，回應「還可以完善什麼功能」這個問題
選的四個方向，另外多做了農曆／節氣顯示。

- **編輯行程**：原本行程只能新增、刪除，現在點行程的「編輯」按鈕可以直接改標題、日期、時間、
  分類、重複設定、備註，桌面版跟手機版 (底部彈出面板) 都支援，改完按「更新行程」就好，不用
  刪掉重加。
- **重複行程**：新增/編輯行程時可以選「每天／每週／每月／每年」重複，之後每個月份檢視都會
  自動展開出所有該出現的日期 (例如每週瑜珈會出現在當月每個週一)。目前是「整串一起改」的
  設計 — 編輯或刪除會套用到整個重複系列，還沒做到「只改某一天」的例外，這對個人排程來說
  應該夠用，之後如果需要再加。
- **跨模組自動合併顯示**：行事曆 (包含首頁那個小行事曆卡片) 現在會自動把「帳單到期日」
  (含已繳/未繳狀態)、「待辦事項的截止日」、「人際關係的生日」、「重要日子」全部整合進來
  顯示，不用每個模組分別去看。這些自動整合進來的項目是唯讀的 (不能在行事曆這邊編輯/刪除)，
  點下去會直接帶去對應的原本分頁 (例如點帳單提醒會跳去記帳分頁)，這樣才不會有兩份資料要
  分別維護、資料不一致的風險。
- **農曆日期＋24 節氣**：月曆格子的日期旁邊現在會顯示農曆日期 (例如「初一」「十五」)，
  節氣當天會多顯示一個節氣標籤 (例如「小暑」)，越接近原生行事曆的呈現方式，比照使用者的
  期望調整。這部分是自己寫的天文計算 (太陽黃經、月相週期的簡化公式)，不是查表，已經拿
  近年幾個有名的節日/節氣日期驗證過運算正確；因為這個環境沒有網路可以即時比對權威資料源，
  如果之後發現某一天顯示的農曆日期跟你手機上的行事曆對不起來，麻煩跟我說一聲，我再回頭校正。
- **匯出成 .ics 訂閱**：設定頁新增「訂閱到手機原生行事曆」卡片，按「產生」會給一組專屬網址，
  貼到 iPhone「行事曆」App (設定 → 帳號 → 新增訂閱的行事曆) 或 Google 日曆 (透過網址新增)，
  之後 LifeHub 這邊新增的行程 (含自動整合的帳單/待辦/生日) 會定期自動同步過去，不用兩邊
  各記一次。這組網址本身可以視為半個密碼 — 知道網址的人就能看到你的行程標題，所以不要
  分享給不信任的人；如果覺得網址外流了，按「停用」就能讓舊的網址失效、重新產生一組新的。
  這個訂閱同步的頻率跟時機是由 iPhone / Google 那邊決定的 (通常抓取頻率沒有辦法在使用者端
  設定，官方說法是不保證多快，實際上大概幾小時內會抓到)，這點沒辦法從我這邊測試驗證，
  麻煩你訂閱之後自己確認一下真的有跳出行程。
- 手機版 (390×844) 重新跑過一輪測試：底部彈出面板正確顯示唯讀的帳單/生日項目 (含「前往查看」
  按鈕)、重複行程正確展開並顯示「🔁 每週」標籤、點「編輯」正確把資料帶進表單、按鈕文字換成
  「更新行程」；月曆本體維持鎖高不用滑動。14 個分頁的橫向溢出回歸測試也重跑過，全部正常。

## [0.24.0] - 2026-07-29

行事曆手機版排版重做：使用者傳了一張 iPhone 內建行事曆的截圖，要求比照那種「一整個月固定
在同一個畫面看完、不用滑動」的呈現方式，原本的手機版是月曆格狀 (只有色點看不出內容) + 下面
還有「新增行程」「選定日期行程」兩張卡片要再往下滑才看得到。

- 手機版月曆現在會鎖住可視高度，整個月份 (無論 4~6 週) 都會在同一畫面顯示完，格子高度依照
  當月週數自動平分剩餘空間，不會有多餘空白，也不用往下滑。
- 格子裡直接顯示事項文字色塊 (最多顯示 1 筆＋「+N」)，比原本只有色點更看得出當天在忙什麼，
  比照使用者提供的參考截圖。
- 原本手機版要往下滑才看得到的「新增行程」表單、「選定日期行程」清單，改成點日期或按頂部
  「＋新增行程」時，用跟「更多功能」選單同一套的底部彈出面板呈現，不佔用月曆本體的版面。
  新增的「📍 今天」按鈕可以快速跳回本月並選到今天。
- 桌面版排版沒有改變，維持原本左邊月曆、右邊常駐「新增行程」+「選定日期行程」的兩欄式版面。
- 用 Playwright 在手機尺寸 (390×844) 跑過：6 週的月份、4 週的月份都確認格子有撐滿、沒有
  明顯需要往下滑的空間 (捲動高度只多出個位數 px，是行動裝置常見的極小量測誤差，不是真的需要
  滑動)；深色模式、桌面版兩欄排版也都截圖確認沒有跑版；14 個分頁的橫向溢出回歸測試也重跑過。

## [0.23.2] - 2026-07-29

自我檢查後的小修正：設定頁檢查「目前有沒有開啟背景推播」這件事，原本理論上可能在極少數
情況 (例如瀏覽器擴充功能擋掉 Service Worker 註冊) 卡在「檢查中...」不會消失、按鈕也會被
藏起來讓使用者沒辦法重試；這次加上 5-8 秒逾時保護，逾時就照「尚未開啟」處理，按鈕會正常
顯示、使用者永遠有辦法再按一次重新嘗試。純防禦性修正，不影響正常情況下的行為。

## [0.23.1] - 2026-07-29

小補充：開通背景推播後，設定頁多一個「📨 傳送測試通知」按鈕，可以立刻收到一則測試推播，
不用乾等背景排程最長 30 分鐘才跑一次，方便剛開通、或明天實際用手機測試時馬上確認有沒有
真的收到。也確認過：如果推播服務回應失敗 (裝置端 key 有問題、endpoint 失效等)，會回傳清楚的
錯誤訊息且不會讓伺服器當掉。

## [0.23.0] - 2026-07-29

延續上一版之後使用者問的兩個問題：提醒事項除了做成彈窗式提醒還有沒有別的做法、AI 助理能不能
直接幫忙加待辦事項。這版把兩者都做出來了。

- **背景推播通知 (螢幕熄滅／App 沒開著也收得到)**：個人化設定頁新增「開啟背景推播通知」，
  開啟後帳單快到期、預算超支、待辦逾期、好幾天沒記心情這幾類提醒，會用手機/電腦系統原生通知
  的方式推播出來，不需要開著這個網站的分頁。跟既有的「開啟瀏覽器提醒」不一樣：那個只有分頁
  開著才有用 (前景通知)，這個是真的 Service Worker + Web Push 訂閱。iPhone 需要先「加入主
  畫面」變成 App、系統版本 iOS 16.4 以上才支援，這點還沒辦法在這個開發環境的沙盒裡直接驗證，
  部署後麻煩自己在手機上實際測一次。
  - 技術上：原本想直接裝 `web-push` 套件 (你也同意可以加套件)，但這個沙盒環境連不到 npm
    registry (完全連不上，不是這個套件特有的問題)。於是改成用 Node.js 內建的 `crypto` 模組
    手刻 VAPID 簽章 (ECDSA P-256) 跟訊息加密 (aes128gcm)，效果一樣、沒有多依賴任何套件，
    已經寫了獨立的加解密/簽章驗證腳本自我測試過，確認邏輯正確。
  - 伺服器會在第一次需要時自己產生一組 VAPID 金鑰對存進資料庫，不需要你自己申請或貼上任何
    金鑰。背景排程每 30 分鐘檢查一次，同樣的提醒內容不會重複轟炸 (內容沒變化的話，最多 24
    小時才會再提醒一次)。
  - 一樣沒有新增 `app.mount()`：`src/routes/push.js` 比照 `insights.js`/`feedback.js` 的
    做法併入既有的 `/api` debug 路由，`server.js` 完全沒被動到。
- **AI 助理主動建議加入待辦事項**：跟 AI 助理聊天時，如果你提到一件「之後要做、值得記錄」的
  具體事情 (例如「幫我提醒我禮拜五要交報告」)，AI 回覆下方會多一張「加入待辦事項？」的確認
  卡片，你按「加入待辦」才會真的新增，AI 不會自己偷偷加東西進去。目前這個功能還沒辦法實際
  測試效果，因為還沒有設定真正的 AI 供應商金鑰——等你自己在 Railway 後台加上
  `GEMINI_API_KEY` 或 `ANTHROPIC_API_KEY` 之後就會開始運作。
  - 技術上刻意不用 Gemini / Anthropic 各自不同格式的 function-calling API (維護成本較高)，
    改成請 AI 在判斷合適時，於回覆文字最後面加一行固定格式的隱藏標記，前端解析出來轉成確認
    卡片、拆掉原始標記文字，使用者完全不會看到那行標記本身。
- 首頁側邊欄的提醒紅點 (原本只有「心情陪伴」有) 這次也確認擴大到帳單、待辦兩個分頁，不用
  點進主動提醒清單也能一眼看到哪個分頁有事要處理。

## [0.22.0] - 2026-07-29

補上幾個之前討論過、但後來被其他事情插隊一直沒真的做出來的功能：待辦事項拆解成小任務、
首頁主動式提醒、首頁本週動態。

- 使用者問「之前說的那些能現在做嗎」，我先錯挖了一批其實使用者自己後來已經明確說不要、或
  其實已經做進去的舊功能，被指正「你挖錯了」；重新逐字比對整段對話紀錄跟現有程式碼之後，
  確認唯一一項「當時確實同意、後來也確實沒做出來」的是待辦事項的「拆解成小任務、追蹤進度」——
  這項在 7/27 提過兩次，都被心情模塊的深度開發插隊，一直沒排進度。人際關係的「深一點」其實
  已經有互動紀錄＋重要日期，記帳的預算/超支提醒/圖表也已經都做了，這次沒有重做。
- 待辦事項：任務可以拆解成多筆小任務，各自打勾，任務列表直接顯示「已完成/總數」的進度條，
  不用整筆任務才能算完成或沒完成。
- 首頁新增「主動式提醒」：帳單快到期或已過期還沒繳、本月某分類記帳超出設定的預算、待辦事項
  逾期超過 3 天、好幾天沒記錄心情，這幾件本來都要自己一個一個分頁點進去才會發現的事，現在
  主動列在首頁最上面，都沒有的話這個區塊就不顯示。
- 首頁新增「本週動態」卡片：純程式計算 (不叫 AI，不用等待也不花費) 把心情/記帳/待辦/行事曆/
  日記這幾個模組本週的數字整理成幾句話，資料量足夠時還會多一句「心情分數較低/較高的那幾天
  支出似乎偏高」這種初步觀察，並明講「非絕對因果」避免誤導。
- 這兩個新功能都沒有新增 app.mount()：實作上另外寫一支 `src/routes/insights.js`，比照既有
  `feedback.js` 的作法，把 routes 直接併進已經掛在 `/api` 的 debug 路由裡，`server.js` 完全
  沒被動到。
- 用 Playwright 測過：主動提醒能正確抓到快到期帳單跟超支分類、待辦小任務新增/勾選/刪除跟
  進度條都正確運作、14 個分頁重新掃描過沒有橫向溢出，也重跑了刪除意見回饋、防橫向位移這兩項
  既有回歸測試確認沒有影響。

## [0.21.0] - 2026-07-29

全站視覺改版：「低調高級感」設計語言，取代原本每個頁面各自一套繽紛主題色的做法。

- 使用者看完手機版風格提案後回報「你看這些基本功能都不見了」，也提到「假如有人使用了自訂
  排序，把心情往上移看起來就很違和」，接著具體說明想要的方向：「當然可以學習 iOS，但希望加點
  低調高級感，不要液態玻璃很醜」。這版就是照這個方向把全站重新上色，同時保留 v0.20.1 剛補上的
  手機首頁心情快速評分、待辦預覽等所有功能不動。
- 核心色彩系統整個換掉：背景改成暖白 (`--bg`)，全站只留一個沉穩深綠當主色 (`--accent`)，
  搭配一點點金色 (`--gold`) 只用在少數最重要的地方點綴，不再有「首頁日出橘、心情天空藍、
  AI 助理靛紫、股票翡翠綠」這種每頁各自一種繽紛主題色的做法——現在整站看起來是同一套系統。
- 拿掉所有毛玻璃模糊卡片 (`backdrop-filter: blur`)，側欄、頂欄、各頁面卡片改成實色暖白背景 +
  一條細邊框，這是使用者明確說「不要液態玻璃」之後的直接回應。
- 首頁那顆會依時段變色的多彩旋轉光球 (早晨橘/午後藍/傍晚粉/深夜靛，４種各自一組漸層)，簡化成
  全時段統一的安靜光圈 (主色+金色的細緻漸層，透明度壓低很多)，不再靠色相狂換來做時段區隔。
- 手機首頁小工具拼貼：拿掉每個小工具各自一片飽和漸層滿版底色的做法，改成暖白卡片 + 左側一條
  細細的色彩強調線 (顏色沿用原本的分類色，只是從「整片背景」收斂成「一條線」)。這直接解決了
  使用者說的「自訂排序把心情移到大方塊位置看起來很違和」——不管小工具被拖到哪個位置、哪種大小，
  現在看起來都是同一套安靜的系統，不會再有一塊突兀的粉紅色或天空藍卡片。
- 股票頁「漲紅跌綠」這條台股慣例、心情紀錄的分數色階、图表資料色 (`--series-*`) 全部維持不動，
  這次只調整品牌/裝飾用的顏色，不動任何有意義的資料視覺化顏色。
- 深色模式同步更新成同一套邏輯 (暖調深色背景 + 提亮版深綠主色 + 金色點綴)，不是分別反轉每頁
  原本各自的繽紛色。
- 用 Playwright 截圖比對桌面/手機、淺色/深色模式共 10 幾張畫面 (首頁、股票、心情陪伴、AI 助理、
  手機小工具拼貼含「心情拖到大方塊」情境)，並重跑意見回饋刪除、防橫向位移這兩項既有回歸測試，
  確認這次改版沒有影響任何既有功能。

- **修訂版本 (patch, 0.1.x)**：修 bug、調整文字，不改變資料庫結構、不影響既有功能。
- **次版本 (minor, 0.x.0)**：新增功能，但保持向下相容 (舊資料、舊 API 呼叫方式仍可用)。
- **主版本 (major, x.0.0)**：有不相容的重大改動 (例如砍掉某個 API、改變資料庫結構且無法自動遷移)。

每次要發新版本時，請照下面順序做：

1. 確認 `migrations/` 資料夾內，這次有改資料表結構的話，是否已經新增一支新的
   `00X_描述.sql` (數字比目前最大的編號 +1)。**不要修改已經發布過的舊 migration 檔案**，
   永遠用新增檔案的方式描述改動，這樣舊使用者的資料庫升級時才能正確地「補跑」新的異動。
2. 更新 `package.json` 的 `version` 欄位。
3. 在下面新增一個版本區塊，寫清楚這個版本改了什麼。
4. 佈署新版程式碼、重啟服務 (`npm start`)。伺服器啟動時會自動偵測並套用尚未執行過的
   migration，不需要手動操作資料庫。
5. 佈署後，用 `/api/health` 確認版本號正確、用 `/api/debug` (見 README「除錯」章節)
   確認資料庫migration 都已套用、外部資料源連線正常。

## [0.20.1] - 2026-07-29

新增：手機首頁的心情/待辦小工具，排到「大方塊」位置時會顯示跟桌面版對齊的完整互動內容，
不用點進去才看得到：

- 使用者看了風格提案後回報「電腦版有的功能，手機首頁好像都不見了」，具體舉例是桌面版
  心情卡本來就可以直接在首頁點 1-10 分記錄心情，手機版首頁小工具卻只有一個簡單摘要，
  要點進心情頁才能記錄——這是手機版小工具本來就存在的功能落差，這次補上。
- 心情小工具排到大方塊 (w2x2) 位置時，直接顯示跟桌面版一樣的 1-10 分快速評分按鈕，點了
  馬上存檔，不用離開首頁；排到小方塊/長條位置時空間不夠放 10 個按鈕，維持原本的精簡摘要
  (點擊才會進到心情頁)。
- 待辦小工具排到大方塊位置時，比照桌面版多顯示 1~2 筆待辦事項的標題；排到小方塊/長條時
  維持原本的精簡數字。
- 用 Playwright 測過：把心情拖到第一個 (大方塊位置)，10 個評分按鈕都在、點了會正確存檔；
  把待辦拖到第一個，也正確多顯示 2 筆事項標題；全部 14 個分頁的版面掃描重新測過，沒有
  水平溢出或其他問題。

## [0.20.0] - 2026-07-29

優化：手機首頁小工具拖曳排序，不會再排出「比例失衡」的版面：

- 之前每個小工具的大小 (大方塊/小方塊/長條) 是跟著小工具的身分寫死的 (例如股票永遠是
  大方塊、心情永遠是小方塊)，使用者自己拖曳把小工具往前移動之後，因為大小沒有跟著調整，
  常常會出現「一個小方塊獨自卡在整排最上面」這種視覺上不協調的畫面——這就是這次回報「把
  心情往上移看起來很違和」的原因。
- 這次把「大小」跟「是哪個小工具」拆開：改成不管使用者把哪個小工具排到第幾個，一律照
  「大方塊 → 小方塊 → 小方塊 → 長條」這個固定節奏，每 4 個一輪循環決定形狀。每個小工具
  的內容本來就很單純 (一個數字 + 一行說明文字)，放大方塊、小方塊、長條都撐得起來，所以
  不管使用者怎麼排序、關掉幾個小工具，版面永遠是同一套看起來協調的節奏，不會再排出獨自
  卡住、比例失衡的畫面。
- 用 Playwright 測過：把「心情」拖到第一個，會自動變成排頭的大方塊 (不再是突兀的小方塊)；
  只開啟其中 4 個小工具、關掉股票和記帳，版面一樣協調、沒有水平溢出。全部 14 個分頁的
  版面掃描也重新測過一次，沒有受影響。

## [0.19.6] - 2026-07-29

修正：使用者回報「右邊那塊白色一直都在」(不是短暫閃一下，是持續存在) —— 這個關鍵字讓
這次終於能在自己的環境裡**穩定重現、實際證明**問題，不再只是猜測：

- 找到根本原因：`.main` (手機模式下真正在捲動的容器) 原本只寫了 `overflow-y: auto`，
  沒有另外寫 `overflow-x`。CSS 規範有一條規則：只要 `overflow-y` 不是 `visible`、
  `overflow-x` 又剛好是預設的 `visible`，瀏覽器就會自動把 `overflow-x` 也當成 `auto`
  處理——等於 `.main` 從一開始就「順便」變成了一個獨立的水平捲動容器，只是視覺上通常沒
  東西需要橫向捲動，才一直沒被發現。只要有任何原因 (手勢、瀏覽器把 focus 的欄位自動捲進
  可視範圍等等) 讓它的水平捲動位置被推離 0，因為切換分頁的程式碼從來沒有把這個位置歸零
  過，它就會「卡住」一直維持在偏移的狀態——這才是「白色一直都在」的真正原因，跟前面幾版
  猜的「捲動回彈」「圖層合成」方向都不一樣。
- 這次不是用猜的：直接寫了一段測試程式，用 JavaScript 把 `.main` 的水平捲動位置設成非 0，
  實際看到畫面內容真的往左跳、右邊真的露出一塊空白——**先重現出問題本身**，再改。
- 修法有兩層，缺一不可：① 明確幫 `.main` 補上 `overflow-x: hidden`，讓它不再是一個可以
  被手勢橫向拖動的容器；② 額外加一道保險——不管上面那條規則以外還有沒有其他原因會讓水平
  捲動位置跑掉，只要偵測到它不是 0，就立刻強制拉回 0，等於從「防止發生」再加一層「發生了
  也馬上復原」。
- 修完後重新測試：用程式把水平捲動位置設成非 0，馬上被拉回 0；用模擬觸控做橫向拖曳手勢，
  畫面完全沒有跟著位移；原本的下拉重新整理功能、全部 14 個分頁的版面掃描，也都重新測過
  一次，沒有受影響。

## [0.19.5] - 2026-07-29

修正：換個角度處理「畫面邊緣偶爾露出一塊空白」——不再只追「為什麼會位移」，改成「就算真的
有殘留的極短暫位移，也不要讓它露出刺眼的空白」：

- 使用者這次直接問「這塊白色的可以讓它不存在嗎」，仔細檢查發現一個很基本、之前一直沒注意到
  的地方：整份 CSS 裡，`body` 有設定跟畫面一致的底色 (漸層背景)，但最外層的 `html` 元素
  完全沒有設定任何背景色。也就是說，只要 WebKit 內部的圖層合成有任何一點點時間差沒完全對齊
  (不管是前面幾版猜的哪一種原因，或是其他還沒抓到的原因)，露出來的縫隙預設就是瀏覽器畫布
  本身的白色——這正是使用者這幾次截圖裡一直看到的那塊「白色」的真正來源。
- 這次直接幫 `html` 補上跟 `body` 同一套底色系統的背景色 (也會跟著淺色/深色模式自動切換)。
  這樣不管未來還有沒有殘留的極短暫位移，露出來的都會是同色系背景，而不是刺眼的白色，實際
  觀感上這塊「空白」就不會再出現了。
- 這個修法跟前面幾版 (v0.19.1～v0.19.4) 不衝突，是疊加上去的最後一道防線：前面幾版持續
  在減少「畫面到底會不會位移」，這一版確保「就算真的有一絲位移，使用者也看不出來」。

## [0.19.4] - 2026-07-29

修正：部署 v0.19.3 之後，使用者又給了更精確的線索——「螢幕右邊會出現上下捲動提示條的
那個位置，靠左一點點，一直往右滑，有機率讓 bug 出現，但不好重現」。這個描述才真正對上了
問題：

- 螢幕最右邊那一小條會出現「捲軸提示」的地方，是 iOS 原生的東西 (不是我們畫的)，那個區域
  也可以直接用手指拖曳。我們自己寫的「下拉重新整理」功能，為了判斷手指是不是在做下拉手勢，
  在 `.main` 整個範圍的 `touchstart`/`touchmove` 都在監聽、都在即時改一個提示文字的位置
  (`transform`)。如果手指剛好從「原生捲軸可以拖曳」的那個窄範圍開始滑動，就會同時有
  兩邊 (原生捲軸的拖曳 + 我們自己的 JS) 在同一時間各自嘗試移動畫面，兩邊打架，才會偶爾
  看到畫面跳動一下——這也可以解釋為什麼很難穩定重現 (要手指剛好壓在那個窄範圍才會發生)。
- 這次修正：下拉重新整理的手勢判斷，直接跳過螢幕最右邊 24px 這一小塊範圍，不去讀那裡的
  觸控事件，把這塊完全讓給原生捲軸處理；另外也加了一個「橫向位移大於縱向位移就直接放棄
  這次下拉手勢」的保護，避免任何非單純下拉的手勢誤觸發這個功能、繼續跟原生手勢搶畫面。
- 用 Playwright 模擬觸控測試過：畫面中間正常下拉重新整理功能不受影響，從螢幕最右邊那條
  窄範圍下滑則確認不會再觸發下拉重新整理的邏輯。

## [0.19.3] - 2026-07-29

修正：部署 v0.19.2 (關掉垂直回彈動畫) 之後，使用者又錄了一次影片證實問題還是存在——
代表「捲動回彈動畫」這個方向從一開始就猜錯了，這次改換方向重新排查：

- 找到另一個更符合症狀的已知 WebKit 問題：`.topbar` (置頂 sticky) 跟 `.bottomnav-mobile`
  (固定 fixed 的底部導覽列) 兩個元素都疊了毛玻璃模糊效果 (`backdrop-filter`)，這種「固定在
  畫面上 + 毛玻璃」的元素放在一個會捲動的容器裡，是 WebKit 很常見會在快速捲動、捲到邊界
  的瞬間，因為圖層合成 (compositing) 重新計算的時機沒抓好，讓整個畫面短暫跟著跳動一下的
  組合——這個問題只有在真正的 WebKit 裡快速滑動才會出現，一般測試工具很難重現，這也是
  為什麼前兩版一直沒抓到。
- 拿掉 `.main` 身上舊版 Safari 專用、新版 WebKit 其實已經不需要、而且已知會加重這類圖層
  問題的 `-webkit-overflow-scrolling: touch`。
- 幫 `.topbar` 跟 `.bottomnav-mobile` 加上 `transform: translateZ(0)`，讓瀏覽器提前把它們
  各自獨立成一個 GPU 合成圖層，不用等捲動當下才臨時決定，避免那個重新合成的時間點造成
  畫面跳動。
- 這類圖層合成問題無法在自動化測試或模擬器裡重現，需要實機驗證；如果這次還是沒解決，
  下一步會請使用者提供「是在滑到哪個確切位置/哪個分頁」發生的更精確描述，改用逐步排除法
  (例如先把毛玻璃效果整個關掉測試，確認方向對不對) 而不是一次改多個地方。

## [0.19.2] - 2026-07-29

修正：手機版快速滑到頂端時，仍有機會看到畫面短暫往右晃一下：

- 使用者提供了螢幕錄影，這次終於能逐格拆解畫面實際發生了什麼事：問題不是「手指真的在
  橫向滑動」，而是快速往上滑、滑到頁面最頂端觸發垂直方向的橡皮筋回彈動畫的那一瞬間，
  WebKit 有機率讓整個畫面連帶短暫往右位移、露出左邊一小塊空白，回彈動畫結束後才彈回
  正常位置——純粹是回彈動畫本身的算圖問題，跟橫向手勢無關。
- v0.19.1 當時只鎖住了水平方向的橡皮筋回彈 (`overscroll-behavior-x: none`)，垂直方向
  刻意保留回彈手感 (`overscroll-behavior-y: contain`)，但這樣不夠——會晃的其實就是這個
  垂直回彈動畫本身。這次把 `.main` (手機模式下真正在捲動的容器) 的回彈行為整個關掉
  (`overscroll-behavior: none`)，滑到頂/滑到底直接停住，不會再有回彈動畫，也就不會再有
  連帶的橫向位移。代價是犧牲掉一點點「滑過頭會彈一下」的原生手感，換取畫面完全不晃。
- 另外也確認過使用者截圖裡「首頁最下面那排交通/日記/心情/帳單圖示」跟這個問題無關，那
  只是首頁「全部功能」捷徑區塊本身的內容，不會滑動也不會晃，純粹是排版位置常被底部導覽列
  擋到一半而已 (是否要調整這塊排版留待使用者確認，這次沒有一併處理)。

## [0.19.1] - 2026-07-29

修正：手機版從畫面中間往右滑，仍有機會出現左右晃動：

- 找到根本原因：手機模式下 `html`/`body` 已經被鎖死不能捲動 (`overflow: hidden`)，
  真正在捲動的容器其實是 `.main`。但先前「關掉橡皮筋回彈」(`overscroll-behavior: none`)
  這個設定只放在 `html`/`body` 上，等於根本沒生效在真正的捲動容器上 —— 這就是為什麼
  即使先前幾個版本都修過相關問題，中間往右滑還是偶爾會晃。
- 這次直接把 `overscroll-behavior` 設定在 `.main` 本身：垂直方向維持正常回彈手感，
  水平方向完全鎖死 (`overscroll-behavior-x: none`)。
- 全站唯一「刻意」允許左右滑動的地方是次分頁列 (`.subnav`，例如交通/旅遊、股票等分頁
  上方的分類列)，這次也幫它加上 `overscroll-behavior-x: contain`，滑到底就停住、
  不會有橡皮筋彈跳感，也不會把手勢往外傳給上層造成整個畫面跟著晃。
- 用 Playwright 模擬觸控手勢 (從畫面中間往右拖曳) 在全部分頁測過，DOM 層級沒有任何
  水平捲動/位移殘留；橡皮筋回彈屬於 WebKit 底層的視覺效果，無法在自動化測試裡完全
  重現，需要實機驗證。跟前幾版一樣，**部署後請把 App 完全關閉重開**才會套用到新的
  捲動設定。

## [0.19.0] - 2026-07-29

新增：管理者可以在「所有回饋」列表裡刪除單筆回饋：

- 每一筆回饋卡片右上角多了一個 🗑 按鈕，點下去就直接刪除那一筆 (跟站內其他刪除按鈕
  一致的做法，不跳原生確認彈窗)，刪除後列表會立刻重新整理。
- 新增 `DELETE /api/feedback/:id`，伺服器端會再檢查一次登入者 email 是否等於管理者
  email，不是只靠前端不顯示按鈕就以為安全；非管理者直接打這支 API 一樣會被擋下 (403)。
- 這支路由沿用跟其他 `/api/feedback` 路由一樣的做法，掛在既有的 `debug.js` 路由檔裡，
  `server.js` 完全沒有變動。
- 用 Playwright 驗證過：管理者刪除後列表數量正確減少；非管理者直接呼叫 API 會被 403
  擋下。也重新跑過一次完整的 14 分頁 QA 掃描，沒有新增的問題。

## [0.18.3] - 2026-07-29

你回報「已經升級上去了，畫面還是長這樣」，傳的截圖是「加入主畫面」的獨立視窗模式
(沒有 Safari 網址列)。這次抓到一個更根本的問題：**手機上很可能一直在用舊版的
app.js/style.css，跟伺服器實際部署的版本對不上。**

- **根因**：伺服器原本完全沒有幫靜態檔案 (`app.js`、`style.css`、圖示等) 設定任何
  `Cache-Control` 標頭。沒有這個標頭的話，iOS 的 WebKit (不管是 Safari 分頁還是加到
  主畫面的獨立視窗) 對靜態檔案的快取policy 相當積極，常常會「伺服器明明已經換成新版本
  了，手機上卻還在用瀏覽器/系統快取住的舊 app.js、舊 style.css」，即使把 App 從多工列表
  滑掉重開也不一定能強制重新抓取——這就是為什麼前面兩次修好的 bug，在你那邊看起來
  「還是這樣」：很可能不是新的 CSS/JS 沒生效，是手機根本還沒真的抓到新的 CSS/JS。
- **修法**：伺服器現在對所有靜態檔案都回傳 `Cache-Control: no-store, must-revalidate`，
  強制瀏覽器/WebKit 每次都要重新跟伺服器要最新的檔案，不能用本地快取。這個專案的檔案都
  很小，不快取對效能影響可以忽略，但能確保「部署新版本後，使用者馬上就是真的在用新版本」，
  不會再卡在舊快取。這是 `src/mini-http.js` 的修改 (`server.js` 完全沒有改動)。
- **這次更新後，麻煩你務必做一次「完全重開」**：把 LifeHub 從多工列表往上滑掉關閉
  (不是切到背景)，再重新從主畫面圖示打開；如果是在一般 Safari 分頁用，建議直接關掉分頁
  重開一個新分頁瀏覽網址，確保是全新載入而不是恢復舊分頁。之後應該就不會再遇到「明明說
  修好了，畫面卻還是舊的樣子」這個情況了。

## [0.18.2] - 2026-07-29

修正「手機網頁版 (一般 Safari 分頁，不是加入主畫面) 也會跑版」——這次拿使用者傳的
Safari 分頁截圖 (看得到網址列 `xxx.up.railway.app`) 才抓到根因，跟前面兩次修的都不是
同一個問題：

- **根因**：iOS Safari 有個內建行為——只要點到的表單欄位 (`<input>`/`<textarea>`/
  `<select>`) 計算後的字體小於 16px，Safari 就會自動把整個網頁放大，方便看清楚要打的字；
  這個放大狀態在單頁式應用 (SPA) 裡常常不會在切換分頁/收起鍵盤之後自動還原，就變成整個
  頁面一直維持放大——畫面看起來像「跑版」，卡片被裁掉一塊衝出螢幕右邊，但其實不是 CSS
  排版本身壞掉，是瀏覽器自己把整頁縮放層級調高了。查了一下，首頁「心情」小工具的快速
  留言輸入框是 12.5px、全站表單欄位預設是 13.5px，兩個都低於 16px 這個門檵，隨便點一個
  都可能觸發。
- **修法**：在手機寬度 (≤760px) 專門把所有表單欄位的字體都鎖在 16px 以上，從源頭讓
  Safari 不會觸發這個自動放大；桌面版不受影響，字體還是原本比較精緻的 13.5px。
- 用 Playwright 確認過：手機寬度下，登入表單、首頁心情快速留言框、意見回饋彈窗的輸入框
  字體都已經是 16px；桌面寬度下這幾個輸入框字體維持原本的 13.5px 沒有被改動。也重新跑過
  一次完整的 14 分頁 + 子分頁 QA 掃描，沒有新增的問題。

## [0.18.1] - 2026-07-29

修正「畫面還是會像之前那樣歪一邊/晃」——這次是拿使用者傳的實機截圖才抓到根因，
跟 v0.16.2 修的橡皮筋回彈是不同問題：

- **根因**：v0.16.1 為了讓「側滑返回上一頁」在一般 Safari 分頁裡變成「回到上一個分頁」，
  改成每次切分頁都往瀏覽器 history 疊一筆新紀錄 (`pushState`)。但實測發現 WebKit 的側滑
  返回手勢，「預覽動畫」是設計給真的換了一個網頁的情境用的——對這種純粹用 JS 疊出來、
  其實還是同一個網頁的分頁切換，動畫沒有正確的「上一頁畫面」可以顯示，手指滑到一半常常
  會卡住、整個畫面往右歪一塊、左邊被裁掉一塊，就是這次截圖看到的樣子。
- **修法**：切分頁改回用 `replaceState` (取代網址列目前這筆紀錄，不新增一筆)，這樣
  history 深度不會再因為切分頁而增加，WebKit 也就不會再去嘗試那個「找不到正確上一頁
  畫面」的側滑預覽動畫，這個歪一邊/卡住的問題就不會發生了。網址列的 hash 還是會正常跟著
  目前分頁換 (重新整理網頁會停在正確分頁)，只是不會再讓側滑手勢有東西可以滑。
- **取捨**：拿掉的是「在一般 Safari 分頁裡側滑可以在 LifeHub 內部的分頁之間切換」這個
  小功能，換回的是不會再有歪掉/卡住的畫面問題。這個功能拿掉不影響「加入主畫面」的獨立
  視窗模式，因為那個模式蘋果系統本來就完全不提供側滑手勢，之前這功能在那個模式裡本來就
  沒有作用，等於是拿掉一個原本就用不到、又會出問題的東西。
- 用 Playwright 確認過：連續切換 6 個分頁後 `history.length` 完全沒有增加 (修好之前每切
  一次就會多一筆)；也重新跑過一次完整的 14 分頁 + 子分頁 QA 掃描，畫面沒有新的溢出或
  卡住問題。

## [0.18.0] - 2026-07-29

你問「還有什麼需要調整」之後，選了四個方向一起做：安全風險檢查、忘記密碼、深色模式、
更滑順的原生 App 手感。這個版本一次把四個都做完：

**安全風險檢查 (只是檢查，沒有改程式)**：用 Railway MCP 查過正式站環境變數，
`SESSION_SECRET`、`DEBUG_TOKEN`、`GEMINI_API_KEY` 都已經設定成真正的值 (不是還停在
`.env.example` 的預設字串)，AI 助理在正式站其實已經可以正常運作 (跟本機沙盒沒設金鑰是
兩回事)。管理者帳號 (能看到「所有回饋」) 目前純粹用 email 判斷，還沒有實際去正式站
用 910228jason@gmail.com 註冊過的話，理論上任何人都可能搶先註冊那個 email——建議儘快
去正式站完成註冊，把管理者身份鎖定下來。

**忘記密碼**：這個專案沒有接寄信服務，沒辦法做「寄重設連結到信箱」，改用「備用重設碼」
取代：

- 註冊時 (以及之後在設定頁按「重新產生」時) 會給一組只顯示這一次的高強度隨機碼
  (格式像 `A7K9-QX3M-...`)，畫面會用彈窗提醒截圖/記下來，並附複製按鈕。
- 忘記密碼時，登入畫面新增「用備用重設碼重設」連結，填 email + 重設碼 + 新密碼即可
  自己重設，不用等人幫忙。
- 重設碼是「單次有效」——用過一次就失效，逼你之後要重新產生新的一組，不會被重複使用。
- 資料庫只存重設碼的雜湊值 (跟密碼用同一套 scrypt 雜湊)，明碼只在產生的當下出現一次。
- 這次更新之前就註冊的舊帳號一開始沒有重設碼，登入後到設定頁按「重新產生備用重設碼」
  就能補一組。
- 順手修掉一個小 bug：之前登出後如果剛好停在「註冊」畫面，重新登入時會卡在註冊表單、
  看不到登入表單 (也就看不到「忘記密碼」連結)——現在登出一律回到登入畫面。

**深色模式**：新增「外觀」設定 (淺色／深色／自動跟隨系統)：

- 全站顏色本來就幾乎都是走 CSS 變數 (`--bg`/`--text-*`/`--flat-*`...)，深色模式只要
  覆蓋這組變數即可，不用整份樣式重寫；另外針對十幾個「半透明白玻璃卡片」(側欄、股票卡、
  首頁時段光球卡、AI 助理卡等) 額外補了深色版玻璃感，不會變成「深色底但卡片還是刺眼的白」。
- 新帳號預設「自動」，跟著手機/電腦系統設定走；已經手動選深色/淺色的人不會被系統設定
  切換打擾。
- 用 Playwright 在系統深色模式下把 14 個分頁都掃過一次，確認沒有新的版面溢出或錯誤，
  也截圖比對過首頁、股票、AI 助理幾個視覺元素比較複雜的頁面。

**更滑順的原生 App 手感** (呼應你之前提過不希望「生硬」的方向)：

- 分頁切換加了很輕的淡入位移動畫，不是直接「跳」過去 (有開啟「減少動態效果」系統設定
  的人會自動關掉這個動畫，不影響操作)。
- 首頁摘要卡片、股票指數卡、首頁小工具的「載入中」文字換成會閃爍的骨架屏色塊，視覺上
  比較不會有「卡住了」的感覺。
- 手機獨立視窗模式下加了下拉重新整理：在頁面最頂端往下拉超過一定距離放開，會重新整理
  「目前這個分頁」的資料 (桌面版網頁本來就不是這種捲動方式，這個手勢只在手機生效)。
- 首頁小工具「自訂」面板可以拖曳排序了 (按住 ⠿ 圖示拖曳)，順序會存起來、下次登入還在；
  這個用的是 prefs API 裡原本就存在、但一直沒有前端功能在用的 `moduleOrder` 欄位，沒有
  加新的資料庫欄位。
- 用 Playwright (滑鼠拖曳模擬 + 合成觸控事件模擬下拉手勢) 驗證過：分頁切換動畫有觸發、
  骨架屏會正常被資料取代、拖曳排序真的會改變順序並且存進 prefs、下拉手勢會正確觸發
  「重新整理中」再回到閒置狀態。也重新跑過一次完整的 14 分頁 + 子分頁 QA 掃描，確認沒有
  新增的問題。

`server.js` 這次同樣完全沒有變動，部署方式不變。

## [0.17.0] - 2026-07-29

新增「意見回饋」功能：任何登入的使用者都能隨時送出問題回報/建議/其他想法，但只有你的
帳號 (管理者) 看得到全部人送出的內容，其他人看不到列表、也看不到別人寫了什麼：

- **管理者判斷方式**：用登入 email 判斷，寫死在 `src/services/admin.js` 的
  `ADMIN_EMAIL` (預設就是 910228jason@gmail.com，之後想換人只要設定環境變數
  `ADMIN_EMAIL` 覆蓋，不用改程式碼)。刻意選這個做法而不是「把帳號密碼給我」——
  帳號密碼共用本身就是資安風險，而且完全沒必要，用自己平常登入的帳號就能自動判斷。
- **使用者怎麼送出**：右上角新增一顆 💬 圖示按鈕 (跟大頭貼、登出按鈕同一排)，App
  版、電腦版網頁都看得到、都是同一顆按鈕。點下去用彈窗表單 (跟「更多功能」共用同一套
  底部彈窗元件) 讓使用者選分類 (🐛 問題回報／💡 建議／💬 其他) 並輸入內容，送出後彈窗
  自動關閉、跳出「已收到」提示，不用多一個獨立頁面。
- **管理者怎麼看**：在「個人化設定」頁多一個「📨 所有回饋」卡片，只有管理者帳號登入時
  才會出現這張卡片 (不是「卡片在但沒資料」，是一般使用者連卡片本身都看不到)，列出每一筆
  回饋的分類、送出者姓名/email、時間、內容，最新的在最上面。
- **技術細節**：新增 `feedback` 資料表 (migration `015_feedback.sql`)；新增
  `POST /api/feedback` (登入即可送出) 與 `GET /api/feedback` (伺服器端會再檢查一次
  登入者 email 是否等於 `ADMIN_EMAIL`，不是只靠前端不顯示卡片就以為安全)。這兩支
  路由沒有另外用 `app.mount()` 掛在 `server.js`——因為部署流程是把 zip 內容貼進
  GitHub 網頁編輯器、刻意不去動 `server.js`——而是併進已經掛在 `/api` 的
  `src/routes/debug.js`，效果完全一樣，`server.js` 這次同樣完全沒有變動。
- 用 Playwright 測過：一般使用者送出回饋後在設定頁看不到「所有回饋」卡片、直接打
  `GET /api/feedback` API 會被伺服器擋下 (403)；用管理者 email 登入則看得到卡片跟
  剛剛送出的那筆內容；手機 390px 寬度下新按鈕跟大頭貼沒有重疊、也沒有造成任何水平
  溢出。也重新跑過一次完整的 14 分頁 + 子分頁 QA 掃描，確認沒有新增的問題。

## [0.16.2] - 2026-07-29

修正「加入主畫面後」左右晃動的感覺——這是 v0.16.1 補上「側滑返回上一頁」功能後才出現的
新狀況 (使用者兩次反映類似的晃動問題，第一次是 v0.15.4 修的橡皮筋回彈，這次不是同一個
原因)：

- **根因**：v0.16.1 讓切換分頁會寫入瀏覽器歷史紀錄，這樣側滑手勢才能變成「回上一頁」。
  但這也表示 Safari 開始偵測到頁面「有上一頁可以回」，只要手指從畫面左邊緣往右滑一點點
  (不小心碰到也算)，Safari 就會啟動內建的「側滑返回」預覽動畫，把目前頁面往右拖走一小段、
  底下透出上一頁的畫面——這個動畫本身就是一種「畫面被往旁邊拖動」的感覺，跟 CSS 能控制的
  橡皮筋回彈是兩回事，`overscroll-behavior` 管不到它。
- **修法**：在 `html`、`body`、`#app` 這幾層外殼加上 `touch-action: pan-y`，明確告訴瀏覽器
  「這個頁面只允許上下捲動的手勢」，讓 Safari 不要在頁面上啟用左右側滑的導覽手勢辨識。
  同時把手機版實際捲動的 `.main` 內容區也一併加上 `touch-action: pan-y`，全面鎖死水平方向
  的手勢。
- **保留合理的左右滑動**：頁面裡本來就有「一排可以左右滑動切換」的子分頁列 (例如股票、
  交通與旅遊頁面上方那排 台鐵/機票/住宿... 之類的次分頁按鈕，`.subnav`)，這個是刻意設計的
  橫向滑動 UI，不能被一併鎖住。特別在 `.subnav` 上加了 `touch-action: pan-x`，讓它繼續可以
  左右滑動，只有整個頁面外層被鎖住。
- v0.16.1 補上的「切分頁會記錄瀏覽紀錄、側滑回上一頁」功能本身完全沒有拿掉，還是可以正常
  用返回鍵/側滑回到上一個分頁，這次只是關掉 Safari 那個會有拖曳視覺效果的側滑「預覽動畫」
  辨識，兩者不衝突。
- 重新用 Playwright 在 iPhone 尺寸下把全部 14 個分頁、股票 4 個子分頁 (含個股詳細頁)、旅遊
  6 個子分頁，以及更多選單、自訂小工具面板都重新掃過一次，確認：畫面水平方向依然是 0
  溢出、小工具沒有卡在載入中、底部導覽列在每個畫面都維持在可視範圍內、瀏覽紀錄返回功能
  正常運作；桌面版另外確認排版沒有受影響 (這次改動只影響觸控手勢辨識，不影響版面本身)。

## [0.16.1] - 2026-07-29

修正實機測試回報的三個問題 (v0.16.0 手機小工具拼貼上線後，在真的 iPhone 上測試才發現)：

- **首頁小工具永遠停在「載入中」**：找到 bug 根因——小工具的資料是在它的 DOM 元素被真正
  插進頁面「之前」就去抓，這時候 `document.querySelector` 根本找不到對應元素 (元素還只存在
  記憶體裡)，資料抓回來也沒地方可以放，畫面就一直卡在「載入中」。改成先把元素插進頁面、
  再去抓資料，順序對了就正常了。這是本次最主要、最需要修的 bug。
- **底部導覽列在某些頁面會消失、畫面感覺跑版**：原本整個網頁本身可以捲動 (搭配底部導覽列
  `position: fixed`)，這個組合在 iOS Safari 收合/展開網址列時常常算錯位置，導致導覽列跑到
  畫面外。改成手機常見的「App 殼固定、只有內容區自己捲動」寫法 (外層完全不動，只有中間
  `.main` 內容區用 `overflow-y` 自己捲)，導覽列的定位基準不會再被網址列高度變化影響，不會
  再消失或跑位。桌面版不受影響 (這個改動只在手機寬度生效)。
- **頂端標題跟 iPhone 狀態列(時間/訊號)疊在一起**：「加入主畫面」的獨立視窗模式下，系統不會
  自動幫網頁的內容讓出狀態列的空間，補上 `safe-area-inset-top` 讓頂端標題往下讓開，不會再跟
  時間、訊號、電量疊在一起。
- 另外也一併補上「側滑返回上一頁」的功能：之前切換分頁沒有寫入瀏覽器的瀏覽紀錄，在一般
  Safari 分頁裡側滑手勢等於直接離開整個網站；現在切分頁會記一筆紀錄，側滑會變成「回到上一個
  分頁」。要注意的是，iOS「加入主畫面」的獨立視窗模式下，蘋果本身完全不提供側滑手勢，這是
  系統本身的限制，不是網頁做得到的事，這裡只能修好在一般 Safari 分頁裡使用的情況。
- 用 Playwright 在 iPhone 尺寸下，把全部 14 個分頁、股票的 4 個子分頁 (含個股詳細頁)、旅遊的
  6 個子分頁都重新掃過一輪，確認沒有小工具卡住、底部導覽列在每個畫面都在可視範圍內、也沒有
  新增的錯誤；桌面版同樣重新確認排版沒有受影響。

## [0.16.0] - 2026-07-29

手機版首頁改成「小工具拼貼」風格 (從三個模板提案中選定的方向)，並讓首頁內容可以自訂
(不是每個人都常用股票)。純前端改動 (`public/index.html`、`public/js/app.js`、
`public/css/style.css`)，沒有新增/修改資料表，`server.js` 沒有變動：

- **手機底部導覽列**：側欄選單是桌面設計，14 個分頁在手機上不好點也放不下。窄螢幕
  (寬度 ≤760px，涵蓋所有手機尺寸) 現在改用底部導覽列，常駐「首頁、股票、記帳、待辦」
  四個常用分頁，其餘 10 個分頁收進「更多」彈出選單。桌面版沿用原本的側欄，不受影響。
- **首頁小工具拼貼 (手機版)**：窄螢幕的首頁改成大小不一的彩色小工具卡片 (像 iPhone
  桌面小工具)，一眼看到股票指數、待辦件數、今天心情、本月支出、帳單提醒、行事曆等
  重點資訊，色彩延續各分頁既有主題色；下方「全部功能」用小圖示提供其餘分頁的捷徑。
- **首頁內容可自訂**：首頁右上角「✏️ 自訂」可以個別開關股票／記帳／待辦／心情／
  帳單提醒／行事曆這幾個小工具，關掉的不會出現在首頁，但資料都還在、隨時可以重新
  打開。這份設定跟「個人化設定」頁的「首頁模組顯示設定」是同一份，桌面版的首頁卡片
  也會跟著套用同一份設定 (桌面版原本這個設定沒有真的生效，這次一併修好)。
- 用 Playwright 在 iPhone 尺寸 (390px 寬) 對全部 14 個分頁跑過一輪，確認底部導覽切換、
  「更多」選單導覽、自訂小工具開關都正常運作，桌面版 (1440px 寬) 也確認排版沒有受影響、
  無新增的 JS 錯誤、無水平方向溢出。

## [0.15.4] - 2026-07-29

修正「加入主畫面」變成 App 之後，畫面會左右晃動/有橡皮筋彈跳感的問題。純 CSS 修正，
沒有動到任何功能邏輯：

- iOS 在獨立視窗 (standalone) 模式下沒有瀏覽器邊界吸收滑動慣性，稍微手滑就容易出現整頁
  左右晃動或上下過度回彈的感覺，不像原生 App 那樣「版面固定不會亂跑」。
- `html`/`body` 加上 `overflow-x: hidden`，明確鎖死不會有任何內容需要整頁左右滑動；再加上
  `overscroll-behavior: none` 關掉橡皮筋回彈效果，只保留正常的上下捲動。
- 用 Playwright 在 iPhone 尺寸 (390px 寬) 對全部 14 個分頁逐一檢查，確認水平方向零溢出
  (無任何內容超出畫面寬度)，沒有引入新的排版問題。

## [0.15.3] - 2026-07-29

新增 PWA (Progressive Web App) 支援，讓 iPhone/Android 可以把 LifeHub「加入主畫面」，
用起來跟原生 App 一樣 (獨立圖示、開啟後沒有 Safari 網址列)。純新增靜態檔案，沒有動到
`server.js`、資料庫結構或既有功能：

- 新增 `public/manifest.json`：定義 App 名稱、圖示、`display: standalone` (加入主畫面後以
  獨立視窗開啟，不顯示瀏覽器網址列/工具列)、主題色。
- 新增 `public/icons/icon-180.png`、`icon-192.png`、`icon-512.png`：App 圖示 (LifeHub 品牌
  的靛藍→翡翠綠漸層圓角方塊，"LH" 字樣)，對應 iOS/Android 各種尺寸需求。
- `public/index.html` 新增對應的 `<meta>`/`<link>` 標籤 (`apple-mobile-web-app-capable`、
  `apple-touch-icon`、`theme-color` 等)，這是 iOS Safari「加入主畫面」判斷要不要用獨立 App
  樣式開啟的依據。
- 這次沒有加 Service Worker (離線快取)，因為 LifeHub 的資料都是即時的 (股價、記帳、待辦等)，
  離線快取舊資料反而可能造成混淆；現況是「像 App 一樣的殼」+ 一定要有網路才能用，這對一個
  常態連線的生活助理來說是合理的取捨。

## [0.15.2] - 2026-07-28

回應「股票個股資料展開要滑很久、排版太生硬」的回饋，把自選股改成「列表 + 個股詳細頁」的
Master-Detail 導覽方式 (參考 XQ全球贏家 等專業看盤軟體的設計)，只改 `public/js/app.js` 跟
`public/css/style.css`，資料邏輯、API 都沒有變動：

- **自選股列表改成精簡數字列**：原本點一下股票列會在原地展開一大串資料 (估值、走勢圖、技術
  指標、法人動向、新聞)，需要滑很久才看得完；現在列表只顯示最關鍵的數字 (股價、漲跌、本益比、
  殖利率、均線/法人/KD 簡易徽章)，一眼就能掃過所有自選股。
- **點列表跳轉到個股詳細頁**：點任一檔會切換到專屬這檔股票的詳細頁 (不是原地展開)，用左右
  兩欄的方式重新編排原本一路往下疊的內容 —— 左欄是大字報價 + 關鍵數據 + 均線位置，右欄是
  走勢圖 + 技術指標 + 法人動向 + 新聞，兩欄各自呈現，大幅減少整頁往下滑的距離；頁面上方有
  「← 返回自選股列表」可以回上一頁。
- **釘選多檔比較**：列表跟詳細頁都有 📌 圖示，可以同時釘選多檔股票，被釘選的會在列表最上方
  用精簡數字卡片並排顯示 (股價、漲跌、本益比、殖利率)，方便同時比較多檔關鍵數字，而不會像
  之前一次展開好幾大串資料。
- 手機窄螢幕下兩欄會自動改成單欄堆疊，維持可讀性。
- 這次改動不影響持股、到價提醒、選股工具三個分頁，也沒有動到任何資料庫結構或 API。

## [0.15.1] - 2026-07-28

回應「股票頁還是很醜，要學就學心情陪伴那頁」的回饋，重新設計股票頁的視覺風格
(只改 `public/css/style.css`，功能都沒變)：

- 前一版把股票頁改成跟全站一樣的純灰白扁平風格，看起來像陽春的後台報表，缺乏其他頁面
  (首頁日出橘、心情陪伴天空藍、AI助理靛紫) 那種柔光色塊+玻璃感卡片的質感。這版比照同樣做法，
  給股票頁做了專屬的「翡翠綠+金」主題 (呼應金融/成長的意象)：背景柔光色塊、卡片改回玻璃感
  圓角、次分頁改成翡翠綠漸層的膠囊分頁、標題文字改成深綠色、按鈕改成翡翠綠漸層。
- 「漲=紅、跌=綠」的台股慣例維持不變，只是在徽章上加了一點點光暈，不會被新的主題色搶走意義。
- 這次改動一樣是純視覺調整，資料邏輯、既有功能都沒有受影響。

## [0.15.0] - 2026-07-28

新增一批討論過的實用功能，涵蓋記帳、待辦、人際關係、交通與旅遊四個模組
(新增 migration `014_v015_features.sql`，沒有動到 `server.js`)：

- **可能的固定支出/訂閱偵測**：記帳頁新增一張卡片，依「備註+分類」找出在多個月份都出現、
  金額相近的支出，整理成一份「你可能有的固定支出」清單，是猜測、不是自動下結論，你可以自行確認。
- **淨資產趨勢圖**：記帳頁新增每月「累計結餘」隨時間變化的走勢圖 (不含股票等投資部位市值，
  避免跟股票頁的庫存損益總覽兩邊資料源不一致造成混淆)。
- **重複性任務**：新增任務時可以選「每天/每週/每月」，完成這筆任務時會自動照週期產生下一筆，
  不用每次重新輸入。
- **這週回顧**：待辦事項頁新增一張卡片，純陳述這週完成了幾件、哪些已經過期 3 天以上還沒完成，
  不帶評價字眼。
- **重要日期**：人際關係頁新增比生日更通用的「重要日期」，紀念日、對方的特殊日子都可以記，
  可以不綁定特定聯絡人，每年自動重複提醒。
- **行前打包清單**：交通與旅遊新增分頁，可以套用「通用/海島/寒冷天氣/商務出差」範本，或自己
  新增項目、勾選追蹤。
- **旅遊分帳 (支援多幣別)**：交通與旅遊新增分頁，建立一趟旅程、記錄誰墊了什麼 (可以是不同幣別，
  換算成旅程的基準幣別結算)，系統會自動算出每個人的淨額，並用最少筆數的轉帳建議幫你們打平帳務——
  不管是「一人先墊全部」還是「大家輪流出」都適用，因為記錄的是每一筆花費各自的墊款人，不是整趟
  旅程綁定一種模式。

開發過程中順便抓到並修掉兩個既有的小 bug：
- `el()` 畫面工具函式處理 `checked`/`selected`/`disabled` 這類布林屬性時，設成 `false`
  還是會被瀏覽器當成「有設定」而顯示成選取/勾選狀態 (HTML 布林屬性的通病：只看有沒有這個屬性，
  不看值)。已修正為值是 `false` 就不設定這個屬性，行前打包清單跟個人化設定頁的模組開關都受惠。
- 收支趨勢圖等折線圖工具函式原本假設數值一定是非負的，淨資產在虧損時繪圖會整個跑版、Y 軸出現
  奇怪的重複數字；已修正成能正確處理負值，收支等原本非負的圖表畫法不受影響。

## [0.14.0] - 2026-07-28

回應「就算大改UI也沒關係，希望專業一點、不要零零散散」的回饋，把上一版只給股票頁用的
「專業風格」主題，變成整站共用的預設樣式 (只改 `public/css/style.css`，沒有動任何
JS 邏輯或資料庫，所有頁面的功能都不受影響)：

- **卡片統一**：所有頁面的卡片 (`.card`) 改成同一套扁平風格——純白背景、細邊框、極淺陰影，
  標題下方統一加一條分隔線，不再是玻璃霧面 + 圓角光暈的風格，資訊看起來更「同一套系統」。
- **次分頁統一**：交通與旅遊、股票等頁面內的次分頁 (自選股/我的持股/台鐵高鐵/機票…) 全部改成
  底線式切換，不再是圓角按鈕群組。
- **標籤/徽章統一**：全站的狀態標籤改成方角小標籤 (不是聊天泡泡式的圓形 pill)；股票頁「漲=紅、
  跌=綠」的台股慣例維持只在股票頁生效，其他頁面「正向=綠色」的原本意涵不受影響，兩者不會互相
  干擾。
- **數字對齊**：金額、統計數字等統一使用等寬字元 (tabular-nums)，直向比較時對齊更整齊。
- **列表/資料列統一**：任務、聯絡人、股利、交易紀錄等清單樣式統一成細分隔線列表，拿掉原本不一致
  的圓角泡泡感。
- 這次改動是純視覺層級調整，所有既有資料、功能操作方式都沒有改變。

## [0.13.3] - 2026-07-28

回應「股票頁面想要更專業、不要零零散散」的回饋，整個股票頁重新做視覺設計 (只改
`public/css/style.css` 和 `public/js/app.js`，不涉及資料庫或 `server.js`)：

- **新增專屬「專業風格」主題**：股票頁 (`#tab-stocks`) 套用一組獨立的視覺參數——更扁平的卡片
  (細邊框、幾乎不用陰影)、方形而非圓角膠囊的標籤、底線式的分頁切換 (取代原本的圓角按鈕分頁)，
  整體看起來更像專業看盤軟體、而不是聊天泡泡風格的其他頁面。
- **數字全面改用等寬對齊 (tabular numbers)**：股價、報酬率、本益比等所有數字統一用等寬字元，
  直排比較整齊、掃視比較快。
- **選股結果改回表格呈現**：因為 `el()` 工具函式已經加了防呆 (見 0.13.2)，現在可以放心用真正的
  `<table>` 呈現選股結果 (股票／收盤價／漲跌／本益比／殖利率／股價淨值比，數字統一靠右對齊)，
  比上一版的卡片列表更適合同時比較多檔股票。
- **持股總覽改用分隔式數據列**：總市值/未實現損益/已實現損益/累計股利收入，從原本的方塊網格
  改成一列用分隔線隔開的統計條，更接近真實券商 App 的呈現方式。
- 這次改動全部是畫面層級的調整，資料邏輯、API 都沒有變，既有資料不受影響。

## [0.13.2] - 2026-07-28

修掉選股工具的當機錯誤，順便把結果畫面改乾淨：

- **修 bug**：篩選結果的「漲跌」欄位直接把數字塞進畫面元件，沒有轉成文字，導致瀏覽器丟出
  `Failed to execute 'appendChild'...` 整個篩選結果顯示不出來。已修正，並且把共用的 `el()`
  畫面元件工具函式加了防呆 (以後不管哪裡不小心塞數字/布林值進去，都會自動轉成文字，
  不會再讓整頁噴錯)。
- **選股結果改用卡片列表**：原本是一個塞很多欄位的表格，改成一行一檔股票的卡片列表——
  股票名稱/收盤價/漲跌在左边一眼看到，本益比/殖利率/股價淨值比用小圓標籤放右邊，
  比表格更清爽、也更好在手機寬度上閱讀。

## [0.13.1] - 2026-07-28

回應「幫另一半記錄生理週期，但不知道記錄了要幹嘛」的回饋，新增一個完整的用途：

- **新分頁「陪伴另一半」**：側邊欄新增一個獨立分頁 (`public/index.html` 新增 nav-item + tab-panel，
  純前端調整，不需要改 `server.js`)。裡面有三塊：
  1. 「目前狀態」：用你幫她記錄的經期資料估算目前大概在哪個階段 (月經期/濾泡期/排卵期/黃體期)，
     是否在經前觀察窗，並附上對應階段「可以怎麼體貼陪伴」的提示文字——語氣刻意保守 (「有些人可能」)，
     不做斷言、不是醫療建議，把最終判斷留給使用者自己觀察。新增 `GET /api/mood/cycle/partner-status`
     (只吃 tracking_for='partner' 的資料，跟使用者自己的完全分開算)。
  2. 「記一筆」：記錄月經開始日，可以順便寫一句觀察備註 (沿用既有的 `symptoms` 欄位)。
  3. 「陪伴日誌」：把記錄過的經期跟備註依時間排列成一份日誌，這樣「寫了要幹嘛」就有答案了——
     不再只是存進資料庫沒有下文。
- 心情陪伴頁原本的生理週期卡片也補上同一個備註欄位，「記錄自己的」也能順便寫東西了。

## [0.13.0] - 2026-07-28

完善股票模塊。上網查了一下市面上的股票追蹤 App (Benzinga、ChartingLens 選股/監控類文章，
以及台灣的「台股記帳本」「三竹智選股」等 App) 大概都在做哪些功能，抓出四個方向動手做：

- **庫存損益追蹤 (新增「我的持股」分頁)**：新增 `stock_transactions`／`stock_dividends`
  資料表，記下買賣進出跟股利發放，損益採「移動加權平均成本法」在伺服器端計算 (均價、
  已實現/未實現損益、累計股利收入)，不用自己拿計算機算。市值/未實現損益需要當日收盤價，
  查不到報價時會誠實顯示「部分報價暫缺」而不是顯示一個少算的數字。
- **更多技術指標**：原本只有均線描述，這次加了 RSI、KD、MACD、成交量趨勢 (`GET /api/stocks/technicals/:code`)，
  自選股展開後會看到完整區塊。跟均線指標同一個原則：只描述「近期相對位置」，不用「買進/賣出」
  這種字眼，也不做價格預測。
- **法人買賣力道「量有沒有縮」**：新增 `GET /api/stocks/institutional/:code/volume-trend`，
  比較近3日跟前3日的法人買賣力道總量，直接標出「收斂/放大」，回應使用者特別想看的這個指標。
- **到價提醒 (新增「到價提醒」分頁)**：新增 `stock_price_alerts` 資料表，可以設定漲到/跌到
  某個價位。因為沒有背景推播機制，是「打開 App 按一下檢查」的模式，不是即時通知，畫面上有
  清楚說明。
- **簡易選股/篩選工具 (新增「選股工具」分頁)**：用證交所公開的全市場估值資料 (本益比/殖利率/
  股價淨值比)，依使用者指定條件排序整理 (例如「殖利率由高到低」)，明確定位是「資料篩選工具」
  不是「選股建議」。
- 股票頁改成跟交通頁一樣的分頁式介面 (自選股／我的持股／到價提醒／選股工具)，資訊分開顯示，
  不會全部塞在同一個畫面。
- 財務目標、股票交易/股利/提醒資料都補進 `/api/export/all` 的備份匯出範圍。
- 這次新增的路由都掛在既有已註冊的 `stocks.js` 裡，**不需要手動修改 `server.js`**。

## [0.12.1] - 2026-07-28

延續 0.12.0 的討論，做兩個小修正：

- **生理週期新增「幫誰記錄」**：`cycle_entries` 新增 `tracking_for` 欄位 (`self` / `partner`)。
  有些人記錄經期不是為了自己，是想幫另一半記錄方便提醒。這種情況下的紀錄現在會被排除在
  「主動關心」的經前提醒、以及跨模塊模式分析的心情關聯之外——因為那不是使用者自己的
  生理狀態，不該被拿去分析使用者自己的心情。畫面上新增一個「記錄自己的／幫另一半記錄」
  的選項，紀錄清單也會標示出哪些是幫另一半記的。
- **心情陪伴 AI 聊到人際狀況時，語氣改成客觀陳述**：之前提到會讓 AI 認得使用者記錄過的人，
  這次加強了一條原則——使用者聊到跟某人的衝突或摩擦時，AI 不會只憑單方敘述就對另一方
  下評論或直接站在使用者這邊指責對方，而是保持中立、同理使用者的感受，同時提醒這只是
  聽到的其中一種觀點，引導使用者自己思考，而不是幫忙「評對錯」。

## [0.12.0] - 2026-07-28

五個「跨模塊深度功能」+ 表單自動記憶，讓幾個模塊之間開始互相參考彼此的資料，
而不是各自獨立的紀錄本；同時修掉一個既有的排版 bug：

- **首頁摘要（依時段變化，不再永遠是「晨間簡報」一種樣子）**：新增 `POST /api/assistant/briefing`，
  手動按按鈕才觸發 AI（不會每次載入首頁就自動打 API），彙整近期到期帳單、今天行程、
  到期/逾期任務、記帳超支分類，組成一段自然語言摘要。首頁現在會依當下時間分成早晨/午後/
  傍晚/深夜四種樣貌，各自有不同的徽章文字、配色、問候語跟摘要按鈕文字，不會一直長一樣。
- **睡眠 × 心情關聯分析**：`moodPatterns.js` 新增「前一晚睡不到6小時 vs 睡滿6小時以上」的
  心情對比訊號 (接在既有的跨模塊模式分析引擎裡，一樣先算數字再交給 AI 組織成人話)。
- **財務目標追蹤**：新增 `finance_goals` 資料表 + `/api/finance/goals` CRUD。不用手動記錄
  存了多少，是用「設定目標之後的實際收入減支出」自動算出目前進度，並用平均儲蓄速度推算
  大概何時能達成，記帳頁新增「🎯 財務目標」卡片呈現進度條與預估達成日。
- **人際關係「好一陣子沒特別聯繫」+ 社交心情關聯**：`relationships` 新增 `reminder_interval_days`
  欄位 (可依關係類型給不同預設週期：家人30天/朋友45天/同事及其他60天，也可個別覆寫或不設)，
  新增 `/api/relationships/overdue`。這裡刻意只呈現「距離上次記錄的互動幾天」這個中性資訊，
  不會用催促或「應該要」的語氣要求使用者去聯絡誰，聯不聯絡都由使用者自己決定。
  `moodPatterns.js` 也新增「有無社交互動」跟心情的對比訊號，一樣只是單純的數字觀察。
- **任務 × 心情/精力排程建議**：`tasks` 新增 `energy_level` 欄位 (低/中/高)，新增
  `GET /api/tasks/suggest-today`：如果今天有記錄心情，會依「過去在類似心情狀態下、
  不同精力任務的完成率」排出一份「今天建議先做」清單；心情資料不夠時就老實說明、
  退回原本依優先度/期限的排序，不亂猜。
- **心情陪伴 AI 現在認得你在「人際關係」記過的人**：對話時會把姓名/關係/備註整理成
  背景資料交給 AI，讓它在你提到某人的時候能接得上脈絡，但只被動地「認人」，
  不會主動搬出你沒提過的人，也不會對社交頻率發表意見。
- **表單自動記憶 (`<datalist>` 自動完成)**：情緒/情境 (心情陪伴想法重塑練習)、記帳備註、
  待辦事項備註、人際互動備註都改成「保留自由輸入，同時建議之前打過的內容」，
  不用每次重複打一樣的字。
- 修掉一個既有的排版 bug：待辦事項清單的核取方塊 (checkbox) 因為全站 `input{width:100%}`
  的規則被撐成佔滿整列，導致旁邊的任務文字被擠成一行一個字的直排 — 現在已修正。
- 這次新增的路由都掛在既有已註冊的 router 檔案 (`finance.js`/`relationships.js`/
  `tasks.js`/`assistant.js`) 裡，**不需要手動修改 `server.js`**，直接上傳新的 zip 即可。

## [0.11.4] - 2026-07-28

淺色主題第二階段：首頁、AI 助理各自客製設計（其餘 10 個分頁仍是共用淺色基礎，尚未客製）：

- 我的首頁：新增「晨間簡報」主題——暖橘/杏色的日出光暈裝飾，中央的 AI 對話核心球改成日出漸層色（橘→黃→珊瑚紅），四周資訊卡片改成奶油白圓角卡片，跟心情陪伴頁的天空藍做出區隔。頂端新增一個「☀️ 晨間簡報 · 幾月幾日 星期幾」的小徽章。
- AI 助理：整頁重新設計成置中的「智慧對話室」卡片——靛紫色調的漸層對話核心球、柔和的紫色光暈背景、對話泡泡改成靛紫色系。新增 4 個常見問題的快速提示按鈕（規劃行程、推薦食譜、翻譯、記帳分類），點一下會直接帶入輸入框，不用自己想要問什麼。
- 純畫面調整，沒有新增資料庫欄位或 API，這次沒有改 server.js，只需要上傳新的 zip，不用手動改 server.js。

## [0.11.3] - 2026-07-28

全站改成淺色主題，第一階段：共用淺色基礎 + 心情陪伴頁「晴空手札」客製設計：

- 把整站的顏色 token（背景、卡片底色、邊框、文字顏色、陰影、系列色/狀態色）從暗色星空主題全部換成淺色版本，側欄、頂欄、登入畫面、按鈕、輸入框、卡片等共用元件都已經是淺色。其餘 12 個分頁目前套用的是共用的淺色基礎樣式，還沒有各自客製設計（下一階段會逐頁進行，見下方説明）。
- 心情陪伴頁完整做成「晴空手札」風格：天空藍／晨光黃的柔和光暈裝飾、圓角玻璃感卡片、天空藍對話泡泡、玫瑰紅的求助專線徽章，維持上一版「一畫面儀表板、不需要滑動」的排版邏輯不變。
- 移除原本的星空背景動畫（淺色主題不需要），改用柔和的天空藍/黃光暈點綴。
- 順便修掉一個既有 bug：`index.html` 的 `#shell` 元素 style 屬性裡重複寫了兩次 `display`（`display:none; ...; display:flex;`），導致還沒登入時主畫面（側欄跟首頁）其實已經在登入畫面後面偷偷渲染出來、只是被登入卡片擋住看不清楚。這次修掉後，未登入時只會看到乾淨的登入畫面。
- 純畫面調整，沒有新增資料庫欄位或 API，這次沒有改 server.js，只需要上傳新的 zip，不用手動改 server.js。

## [0.11.2] - 2026-07-28

心情陪伴頁改成真正的「一畫面儀表板」，取代上一版的分頁籤：

- 整頁鎖住視窗高度，不需要點分頁籤、也不需要滑整頁——三欄式排版：左欄「今日打卡 / 安心小卡 / 生理週期」、中欄「AI 心靈對話室」(維持整個模塊的主軸位置)、右欄「想法重塑練習、趨勢圖、情緒健康報告、洞察、跨模塊模式、因應工具箱、今天的日記、週回顧、月回顧」。
- 只有右欄 (內容最多、最「深」的工具跟資料) 內部自己捲動，左欄、中欄、整個頁面都不會被撐高；如果左欄内容剛好比較多 (例如安心小卡存了很多筆)，左欄也會自己內部捲動，但不會把任何功能藏起來看不到。
- 求助專線改成放在最上面窄狀態列的小徽章，滑鼠移上去可以看到三個專線號碼，跟主動關心訊息一起隨時都在畫面上，不需要額外捲動或切換分頁籤才看得到。
- 順便修掉一個很小的既有 bug：畫面最下面有個沒定位的提示框元素（`.tooltip-box`）平常會在頁面最底端多墊出約 10px 高度，讓每一頁都多一點點捲動空間；這次把它預設移出畫面外，全站所有頁面都會因此更準一點點。
- 螢幕比較窄 (寬度小於 1200px，例如平板) 時會自動退回原本的直向捲動排版，不會硬擠成三欄。
- 純畫面調整，沒有新增資料庫欄位或 API，這次沒有改 server.js，只需要上傳新的 zip，不用手動改 server.js。

## [0.11.1] - 2026-07-27

心情陪伴頁排版整理：卡片變多之後畫面太雜、要一直滑，改成分頁籤：

- 心情頁拆成 4 個分頁籤：「今日打卡」「陪伴對話」「趨勢與洞察」「深度工具」，每個分頁籤只放同一類功能，畫面乾淨很多。
- 主動關心 banner、開啟瀏覽器提醒、求助專線資源，維持在分頁籤外面、切換分頁籤時都看得到，不會被藏起來。
- 求助專線從一張大卡片改成一行式的窄 banner，一樣隨時看得到，但不佔太多畫面高度。
- 想法重塑練習表單改成兩欄排版、精簡欄位提示文字；安心小卡跟想法記錄清單加上內部捲動，避免筆記變多以後把整頁撐得很長。
- 「今日打卡」「陪伴對話」「趨勢與洞察」這三個分頁籤在常見螢幕尺寸下已經不需要滑動；「深度工具」分頁籤因為想法重塑練習表單本身內容較多，仍會有少量捲動空間。
- 純畫面調整，沒有新增資料庫欄位或 API，這次沒有改 server.js，只需要上傳新的 zip、不用手動改 server.js。

## [0.11.0] - 2026-07-27

新增生理週期紀錄 (選填功能)，讓心情陪伴能考慮經前階段對情緒的影響：

- 心情頁新增一張精簡的「生理週期（選填）」卡片：記錄經期開始日，系統會自動估算目前在週期的哪個階段 (月經期/濾泡期/排卵期/黃體期)，距離下次月經還有幾天；完全選填，不記錄不影響其他功能。
- **主動關心整合**：經前觀察窗 (下次月經前 7 天內) 且沒有更急迫的關心事項時，會顯示一句溫和的提前提醒，跟現有的「連續低分」「太久沒記錄」共用同一套 careMessage 機制，不會同時跳出多個提醒。
- **跨模塊模式分析整合**：如果有記錄週期，分析時會額外比較「經前階段 vs 其他階段」的心情分數，跟原有的月底/週末/行程等訊號一起交給 AI 統整。
- 全模塊資料匯出已同步涵蓋週期紀錄。
- 這次沒有新增 server.js 的路由掛載，只需要上傳新的 zip，不用手動改 server.js。

## [0.10.0] - 2026-07-27

心情陪伴功能串接起來，加上四個一直待做的新模塊：

- **技巧成效總覽**：心情頁新增「我的因應工具箱」，把之前對各技巧的回饋整理成一份總覽，讓使用者能看到系統實際上學到了什麼。
- **打卡後自動串接**：心情打卡時如果分數偏低又選了標籤，存檔後會直接跳出提示，問要不要看技巧建議或寫一筆想法記錄，不用自己想到要去別的卡片找。
- **陪伴對話更主動的開場**：如果使用者清過對話畫面、但長期記憶摘要還在，重新打開對話室時 AI 會主動先開口、自然地接續之前的重點。
- **健康紀錄 (新模塊)**：新增睡眠時數記錄 + 近 30 天趨勢圖，以及服藥清單 + 每日打勾提醒。
- **帳單提醒 (新模塊)**：可以記錄每月固定要繳的帳單，追蹤這個月繳了沒、快到期會提示，首頁新增帳單提醒卡片。
- **人際關係 (新模塊)**：可以記錄聯絡人、生日、互動紀錄，未來 30 天內的生日會列出來提醒。
- **待辦事項 (新模塊)**：可以新增任務、設定優先度與到期日、標記完成，首頁新增待辦事項卡片。
- **全模塊資料匯出**：已同步涵蓋以上四個新模塊的資料。

## [0.9.0] - 2026-07-27

心情陪伴深化第二階段，加上全模塊資料匯出跟站內提醒強化：

- **跨模塊深層模式識別**：新增「重新分析」功能，把心情、行事曆、記帳、日記的資料整理成每日特徵，用程式算出幾組平均數對比（月底/非月底、週末/平日、高支出日/一般日、行程滿/行程鬆、有寫日記/沒寫日記、各標籤出現的日子），只把算出來的統計訊號交給 AI 寫成一段觀察文字，明確提醒這只是初步觀察、不是絕對因果關係。需要至少 20 筆心情紀錄才會啟用，分析結果會快取，不會每次開頁面都重算。
- **CBT 式思考記錄**：心情頁新增「卡住的時候：想法重塑練習」，可以寫下情境、腦中的念頭、支持/不支持這個念頭的證據，並請 AI 幫忙想一句可能更平衡的想法（也可以完全自己寫）。明確定位為自助認知練習輔助工具，不是心理治療或診斷。
- **會學習的因應技巧庫**：選一個心情標籤後可以請 AI 給一個對應的因應技巧，並回饋「有沒有幫助」，系統會記住哪些技巧對這個使用者比較有效，之後推薦會愈來愈準，而不是每個人都看到一樣的建議。
- **這個月的 AI 回顧**：心情頁新增「產生本月回顧」，邏輯類似原本的週回顧，但涵蓋整個月、字數更完整一點。
- **AI 對話記憶強化**：心情陪伴對話每累積 40 則訊息，系統會把最舊的一批還沒摘要過的對話濃縮成 1-2 句重點記憶，之後聊天時會把最近幾筆記憶摘要一起交給 AI，讓它即使看不到很久以前的逐字稿，也還記得聊過的重點。
- **全模塊資料匯出／備份**：個人化設定頁新增「匯出全部資料」，把股票自選、日記、記帳、行事曆、心情陪伴（含安心小卡、思考記錄）等全部模組打包成一份 JSON 檔下載，方便備份或之後搬家使用。
- **站內提醒強化**：側邊欄「心情陪伴」旁邊，只要有需要關心的訊號 (連續低分或太久沒記錄)，就會出現一個小紅點，不用特地點進心情頁才知道；心情頁也新增可以自己選擇開啟的瀏覽器原生通知 (一天最多提醒一次)。

## [0.8.0] - 2026-07-27

心情陪伴深度功能一次補齊，並修掉一個對話記錄的舊 bug：

- **修復對話記錄「卡在很久以前」的 bug**：AI 助理跟心情陪伴的對話記錄，之前是用 `ORDER BY id ASC LIMIT N` 撈「最近」的訊息，但這樣一旦對話超過上限，撈到的永遠是最早的 N 筆，AI 會看不到最近講的內容。改成先用 `ORDER BY id DESC LIMIT N` 撈最新的，再反轉順序，兩邊的上限也從 200 提高到 300。
- **首頁快速心情打卡**：首頁「心情陪伴」卡片裡直接可以點 1-10 分打卡、選填一句話，不用特地點進心情頁，方便懶得多點兩下的人也能隨手記一筆。
- **主動關心**：心情連續偏低（近幾筆平均 ≤4 分）或超過 5 天沒記錄時，首頁卡片與心情頁最上方會出現一句溫和的關心/提醒文字，而不是等使用者自己想到才來用。
- **心情 × 行程 洞察**：心情頁新增「心情 × 行程 洞察」卡片，粗略比對「當天行事曆事項數量」跟「當天心情分數」的關聯，給一句白話的觀察（例如行程比較滿的日子心情平均是否比較低）。
- **這週的 AI 回顧**：心情頁新增「產生本週回顧」按鈕，AI 會根據這週的心情紀錄寫一段 100-150 字、像朋友寫小卡片一樣的回顧文字。
- **安心小卡**：心情頁新增可以自己新增/刪除的「安心小卡」清單，收藏對自己有用的一句話，心情不好的時候可以回來看看。

## [0.7.2] - 2026-07-27

依使用者回饋調整：

- 心情分數改回點擊 1-10 的圓形按鈕，不再是拉桿（前一版改成拉桿，使用者覺得點選比較直覺，改回來）。
- 首頁與心情頁面「感覺很空」的問題：把卡片內容加密、加高，減少畫面下方大片空白。股市摘要卡加上自選股清單、記帳流水卡加上本月分類明細、日記卡改成顯示最近 3 篇摘要（不只今天）、心情頁的「情緒健康報告」新增近 30 天心情一覽的色塊格（類似 GitHub 貢獻圖的呈現方式），AI 陪伴對話框也加高避免看起來空盪盪。

## [0.7.1] - 2026-07-27

心情陪伴頁面改版（依使用者提供的參考圖調整視覺風格），並修正上一版的部署疏漏：

- 版面改成三欄：左側「今天感覺怎麼樣」記錄卡 + 趨勢圖，中間獨立的「AI 心靈對話室」卡片，右側「情緒健康報告」統計卡 + 今天的日記。
- 心情分數改成滑桿選擇（1-10），中間有一顆會即時顯示目前分數的圓形指示。
- 趨勢圖加上柔和的漸層底色（面積圖效果），更接近參考圖的視覺質感。
- 新增「情緒健康報告」統計格：近 30 天平均分數、本月已記錄天數、連續記錄天數、情緒穩定度（依分數波動估算，僅供參考，不是醫學指標）、常見標籤。
- 修正：上一版 (0.7.0) 因為 `server.js` 需要直接更新在 repo 裡才能生效（不是透過 zip），導致心情陪伴的所有 API 一直回傳 404，這版說明文件裡也記錄下來，避免下次忘記。

## [0.7.0] - 2026-07-27

新增「心情陪伴」模組（跟使用者討論過設計方向後的第一版）：

- 每日心情紀錄：1-10 分快速選擇（10 分最好）、可複選標籤（焦慮/疲憊/開心/平靜/煩躁/難過/有動力/孤單）、可寫備註，同一天重複記錄會覆蓋更新，不會累積成多筆。
- 趨勢圖表：近 30 天心情走勢折線圖 + 平均分數 + 常見標籤統計。
- 日記串接：心情頁面會帶出當天的日記摘要，沒寫的話一鍵前往日記頁。
- 首頁「心情陪伴」卡片改成顯示真實資料（今天是否已記錄、近 7 天平均分），不再是預告卡。
- AI 情緒陪伴對話：獨立於一般 AI 助理的對話（不同資料表、不同系統提示），語氣定位在「陪伴傾聽」而非正式心理治療；當偵測到自傷/自殺等危機關鍵字時，伺服器端會強制在回覆附上台灣在地的免費求助專線（安心專線 1925、生命線 1995、張老師專線 1980），不完全依賴語言模型自己記得要講。
- 頁面下方固定顯示「需要立即協助？」卡片，隨時可見這幾個專線，不用等到觸發危機關鍵字才看得到。

## [0.6.0] - 2026-07-27

首頁與整體視覺大改版（星空/極光主題），依使用者提供的參考圖調整：

- 整體改為深色系「星空」主題：背景加上會微微閃爍的星空 canvas 動畫（尊重 `prefers-reduced-motion`），卡片改成半透明玻璃質感 + 微光邊框。
- 側欄改為可展開/收合（點擊左上角按鈕切換，狀態存在瀏覽器本機，重新整理後記得上次的選擇）。
- 首頁重新設計成「左右資訊卡 + 中央 AI 核心」的版面：中央是一顆會呼吸/旋轉光暈的 AI 核心，內建快速提問輸入框（按 Enter 直接送到 AI 助理並自動跳轉對話頁）；左右兩側是股市摘要、行事曆行程、交通與旅遊、記帳流水、日記隨筆等卡片，資料改成即時抓真實資料（不是假資料）。
- 新增「心情陪伴」卡片位置，先以「即將推出」預告呈現，之後心情紀錄/圖表/AI 陪伴功能上線後會直接補上，先讓版面對齊未來規劃。
- 說明：參考圖裡外層卡片是六邊形直接裁切文字/圖表，實際內容較多的卡片（例如清單、長條圖）若硬套六邊形裁切會讓文字被切斷不好讀，因此改用圓角卡片搭配六邊形小圖示徽章來呼應視覺風格，同時維持內容可讀性；六邊形完整造型保留在裝飾性強、內容少的中央 AI 核心上。

## [0.5.4] - 2026-07-24

修正資料庫一直被重置的根本原因：不再依賴環境變數 DB_PATH，改成自動偵測 Volume 掛載目錄：

- 背景：多次確認 Railway 上設定的環境變數 `DB_PATH`（不管是透過工具或後台手動設定）都沒有真的
  傳進正在執行的程式裡 (部署 log 一直顯示「未設定 DB_PATH」)，導致資料庫檔案一直寫在容器內的
  暫存資料夾，每次重新部署就會被清空重來，帳號、日記、記帳資料全部消失。
- 修正：資料庫路徑改成「如果有設定 DB_PATH 就用它 → 否則自動偵測 `/data` 這個資料夾存不存在
  (Railway 掛載 Volume 是在檔案系統層級直接掛上去的，跟環境變數傳不傳得進來無關，用資料夾
  存在與否判斷比依賴環境變數可靠) → 都沒有的話才退回本機開發用的暫存路徑」。這樣即使環境變數
  設定有問題，只要 Volume 有正確掛在 `/data`，資料庫就會自動存到那裡、不會再被清空。
- AI 助理的開機診斷 log 改用跟其他 log 一致的格式 (`logger.info`)，方便跟其他訊息一起查看。

## [0.5.3] - 2026-07-24

記帳備註欄位更明顯 + 行事曆改成一畫面看到所有功能 (桌面) / 手機用色點取代文字方塊：

- 記帳：「備註」欄位改成獨立一整行，加上「📝 這筆消費原因/備註」的標籤跟範例文字提示，
  避免被誤以為是自訂分類欄位而沒注意到 (其實本來就可以自由打字，只是不夠明顯)。
- 行事曆：桌面/大螢幕改成「月曆在左、新增行程表單+選定日期行程都排在右邊」的左右並排版面，
  一個畫面就能看到全部功能，不用再往下滑；手機螢幕窄時會自動改回上下排列。
- 行事曆月曆格子在手機上，原本超過三個字的行程標題文字方塊會被完全隱藏、看不出這天有沒有事，
  現在改成用分類顏色的小圓點取代 (最多 4 個)，點日期一樣能在下方看到當天完整內容，
  格子本身也縮小、間距變窄，一次能看到的東西更多。

## [0.5.2] - 2026-07-24

修正股票法人買賣超查詢很慢的問題 + AI 助理開機診斷 log：

- 股票「法人買賣超」查詢 (`/api/stocks/institutional/:code`) 原本是逐一交易日、依序打證交所
  API (最多序列打到 20~30 次，每次最久等 10 秒逾時)，實測單一檔股票要等超過 30 秒才有回應，
  是股票頁面卡頓的主因。改成先算出候選交易日清單，用 `Promise.allSettled` 平行送出所有請求，
  搭配原本就有的 5 分鐘快取 (多檔股票之間也能共用同一天的快取)，大幅縮短等待時間。
- AI 助理：開機時會印出「有沒有偵測到 GEMINI_API_KEY / ANTHROPIC_API_KEY」以及金鑰長度
  (不印出金鑰內容本身)，方便直接從 Railway 部署 log 確認金鑰是否真的有被容器讀到，
  不用等使用者實際點進 AI 助理頁面聊天才發現「金鑰設定了卻沒作用」。

## [0.5.1] - 2026-07-24

除錯用：開機時印出目前實際使用的資料庫檔案路徑：

- 起因：使用者回報帳號密碼登入失敗 (帳號應該是先前已經註冊過的)，懷疑是重新部署時資料庫被清空重建。
- 加了一行開機 log：`[db] 使用資料庫檔案: <路徑> (來源: 環境變數 DB_PATH / 預設路徑，未設定 DB_PATH)`，
  之後只要看部署 log 就能立刻確認資料庫是不是有正確指到掛了 Volume 的持久化路徑，而不是每次重建
  容器就消失的暫存路徑。這個版本本身不影響任何功能或資料庫結構。

## [0.5.0] - 2026-07-24

AI 助理對話記錄改存資料庫 + 日記搜尋/月曆瀏覽 + 記帳分類與預算提醒 + 行事曆改月曆格狀檢視：

- AI 助理：對話記錄改成存進資料庫 (`assistant_messages` 資料表，見
  `migrations/004_assistant_history.sql`)，不會再因為重新整理頁面或重新登入就消失；
  新增「清除對話」按鈕 (`DELETE /api/assistant/history`)。
- 日記：新增關鍵字搜尋 (`GET /api/diary?q=關鍵字`)，以及月曆瀏覽小工具，有寫日記的
  日期會標示圓點，點日期可切換月份查看。
- 記帳：新增項目時分類改成下拉選單 (餐飲/交通/娛樂/購物/居家/醫療/教育/其他，也可
  選「其他」自訂輸入)，不再是自由輸入文字；新增「預算設定」卡片，可依分類設定每月
  預算上限，超支會用進度條顏色提醒 (綠/黃/紅)。
- 行事曆：改成月曆格狀檢視 (取代原本的清單)，事件新增「分類」欄位 (飲食/電影/運動/
  工作/約會/旅遊/其他，見 `migrations/005_calendar_category.sql`)，每個分類用固定
  顏色的小方塊呈現在月曆格子裡；點日期可看當天完整行程。收藏的火車車次新增「加入
  行事曆」按鈕，選日期後一鍵建立對應的行事曆事件 (分類自動歸類為「旅遊」)。
- 修正時區相關的小 bug：原本判斷「今天」與「本月」用 UTC 時間換算，在台灣時區
  (UTC+8) 接近午夜時可能會差一天/一個月，已改用本機時區計算。

## [0.4.2] - 2026-07-24

自選股改成可收合摘要 + 支援用公司名稱搜尋 + AI 助理支援免費的 Gemini：

- 自選股新增輸入框改成「代號或公司名稱都可以」(例如打「華邦電」或「2344」都找得到)，
  新增 `/api/stocks/resolve/:query` 解析用；找不到時會用 toast 提示，不會再整張卡片
  卡在錯誤訊息。
- 自選股卡片改成「收合摘要列 + 展開細節」：預設收合，只顯示股價/漲跌與幾個關鍵數字
  (本益比、殖利率、均線位置、法人動向)；點一下才展開看完整的走勢圖、法人分項、新聞，
  避免加了好幾檔以後畫面一路往下拉。剛加入的股票會自動展開一次。
- AI 助理新增 Gemini 支援：設定 `GEMINI_API_KEY` (免費，前往
  https://aistudio.google.com/app/apikey 免費申請，不需要信用卡) 即可使用；
  也保留原本的 `ANTHROPIC_API_KEY` 選項 (品質較好但採用量計費)，兩個都設定時優先用
  免費的 Gemini。

## [0.4.1] - 2026-07-24

交通與旅遊分頁重新整理 + 正式上線的自我修復機制：

- 「交通與旅遊」原本把火車時刻、機票、住宿、行程規劃 4 個功能全部堆疊在同一長頁，
  現在改成上方分頁籤 (台鐵/高鐵、機票、住宿、行程規劃)，一次只顯示一個功能區塊，
  畫面更乾淨、不用一直往下滑。
- `server.js` 新增內建的自我修復/自我更新機制：開機時若專案根目錄有
  `lifehub-prototype.zip`，會自動解壓縮覆蓋一次專案檔案，純用 Node.js 內建模組
  實作、不依賴外部套件。這是為了因應「透過網頁手動上傳到 GitHub 時，瀏覽器有時
  不會把子資料夾一起送出」這個部署情境而加的保險機制，之後要更新畫面/功能，
  只需要替換這個 zip 檔即可，不需要再手動一個個資料夾上傳。

## [0.4.0] - 2026-07-23

股票深度大改版 + 機票深連結修正 + 全站 UI 視覺優化：

- 股票 (參考 Yahoo 股市資訊密度大幅擴充)：
  - 新增台股加權指數 (TAIEX) 大盤卡片：`/api/stocks/index/taiex` (證交所官方 FMTQIK)。
  - 新增個股估值資料：本益比/殖利率/股價淨值比 `/api/stocks/valuation/:code`
    (證交所官方 BWIBBU_ALL)。
  - 個股卡片新增「開盤/最高/最低/成交量/振幅」數據格與漲跌幅百分比。
  - 新增近 40 個交易日「收盤走勢 + MA5 + MA20 均線」互動圖 `/api/stocks/history/:code`。
  - 三大法人買賣超拆解為「外資/投信/自營商」分項 (最新交易日 + 近15日累計)，
    資料自證交所 T86 動態欄位解析。
- 機票/訂房深連結修正 (回報「點過去跳錯誤」)：
  - 實測確認長榮 `/zh-tw/booking/` 已 404，改連官網首頁。
  - Skyscanner 改用正確的 `/transport/flights/{IATA}/{IATA}/{yymmdd}/` 格式，
    新增 60+ 常用城市中文名→IATA 機場代碼對照表；查不到代碼時誠實降級。
  - 每個連結標示「已帶入條件」或「需自行輸入」徽章，不再假裝所有連結都能預填
    (航空公司官網刻意不開放帶參數訂票網址，需商業合作 API)。
- UI 全面優化：新版設計系統 (漸層品牌色、卡片陰影、hover 效果、聊天氣泡、
  provider 清單頭像、kv 數據格、hero 首頁、深色模式配色重整)；台股慣例
  漲=紅/跌=綠；首頁快捷卡涵蓋全部六個模組。

## [0.3.0] - 2026-07-23

交通、股票、AI 助理三大方向的大幅擴充，回應「深連結訂票」「車次時間篩選+收藏」
「股票要更詳細」「加一個能聊天的 AI 助理」等需求：

- 交通／機票／訂房：
  - 新增「深連結」服務 (`src/services/deeplinks.js`)：輸入出發地/目的地/日期(或城市/入住退房)，
    直接產生 Google 航班、Skyscanner、長榮/華航/虎航、Booking.com/Agoda/Trip.com/AsiaYo
    等各家網站的預填搜尋連結，點下去就帶好查詢條件。明確告知這不是真正比價 API。
  - 台鐵/高鐵時刻查詢新增 `to`(目的站)、`date`、`timeFrom`/`timeTo`(時間範圍)、`trainNo`
    (車次搜尋) 篩選參數，回傳每筆車次含到達時間與票價(估)。
  - 新增「收藏車次」功能 (`favorite_trains` 資料表 + `/api/travel/train/favorites` CRUD)。
  - 智慧行程規劃新增 `CURATED_SPOTS` 精選推薦清單 (`src/services/itinerary.js`)：目前收錄
    「台中」「新北」兩城市，整理自公開旅遊/美食部落格的景點/美食/休閒建議 (質化描述，
    非即時 Google 評分，因未串接 Google Places API 金鑰)。
- 股票：
  - 新增均線描述性指標 `/api/stocks/indicator/:code` (MA5 vs MA20，僅描述近期相對位置，
    非價格預測 — 刻意不做「未來走勢預測」功能，原因詳見 `src/services/twse.js` 註解)。
  - 新增費城半導體指數 (SOX) `/api/stocks/index/sox`：使用 Yahoo Finance 非官方端點，
    已明確標示 `isUnofficialSource`。
  - 新增相關新聞 `/api/stocks/news/:code`：串接 Yahoo奇摩股市官方 RSS。
- 新增行事曆 (`calendar_events` 資料表 + `/api/calendar` CRUD)：可依日期/時間新增行程。
- 新增 AI 助理聊天 (`/api/assistant/chat`)：伺服器端呼叫 Anthropic Claude API (需於 `.env`
  設定 `ANTHROPIC_API_KEY`)，涵蓋日常聊天/生活規劃/股票解讀/翻譯等主題，未設定金鑰時
  會提供清楚的「尚未設定」提示訊息而不是報錯。
- 新增資料庫 migration：`002_favorites_and_prefs.sql`、`003_calendar.sql`。
- 展示預覽版 (單檔 HTML) 同步更新，前端以模擬假資料呈現以上所有新功能，方便不架伺服器
  也能體驗完整介面與互動。

## [0.1.0] - 2026-07-23

初版原型，包含：

- 帳號系統 (註冊/登入/登出)，App 與網站共用同一組帳號與資料庫，個人化模組顯示設定。
- 股票：串接證交所公開資料 (每日收盤 + 三大法人買賣超)，含規則式參考訊號 (非投資建議)。
- 交通：串接交通部 TDX 平台查詢台鐵/高鐵時刻與動態 (需自行申請免費金鑰，否則使用示範資料)。
- 機票+住宿比價：目前為示範資料，架構已預留正式 API 串接位置。
- 智慧行程規劃：規則式行程草案產生器。
- 日記：依日期記錄，支援從本機文字檔匯入。
- 記帳：手動輸入收支、分類統計圖表、月度趨勢圖、預算設定。
- 系統：零外部 npm 依賴 (只用 Node.js 內建模組)、SQLite 資料庫 + 自動 migration、
  結構化 log 與 `/api/debug` 除錯端點、`/api/health` 健康檢查。
